/**
 * app/page.tsx
 *
 * Route '/' is served as legacy static HTML via a beforeFiles rewrite in
 * next.config.js ('/' → '/HTML/index.html'). That runs before this route
 * matches; this file remains a stub until the React landing replaces it.
 */
export default function Home() {
  return null;
}
