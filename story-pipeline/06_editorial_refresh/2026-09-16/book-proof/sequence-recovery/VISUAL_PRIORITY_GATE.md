# Prospective visual priorities: decision gate

2026-09-24. Guy accepted continuing after the push. Codex implements in the same
task, sole writer of `codex/r3b1b-semantic-recovery-m1` at
`C:/GNart/Work/sh-r3b1b-semantic-m1`, base `e491c9bc`. Local/origin equal and clean.
Protected d53b / accepted-intent-wave-2 worktrees remain read-only and clean at
768ccb2f / 63ccb484. No new task, deployment, push or provider calls.

## Observed / expected / cause

Every one of the eight shared QA categories currently blocks on any defect.
Plans have invariants but no separate representation of optional decoration.
LOW failed a spoke count; MEDIUM failed a height ratio. Neither measurement
establishes model superiority or licenses demoting either historical failure.
Expected: author optional nonfunctional details before generating pixels, record
deviations separately, retain every mandatory defect and uncertainty as blocking.

## Scope and stop-check

General opt-in local owner **sample** path, all stories. Source/plan-bound separate
preference asset, validated before images; new QA shape and context identity;
same policy through render, judge, repair, persistence and same-scene predecessor.
Legacy omitted-policy behavior/bytes stay unchanged. Automatic calibrated preview
and customer pipeline do not opt in. No accepted source or historical plan edited.
One focused code milestone plus tests/current/handoff. No model/quality changes.

Narrow reversible assumption: only nonfunctional surface decoration is eligible.
Scale ratios, material, functional parts, identity, anatomy, physical relations,
custody and source facts remain required. Existing structured invariants cannot
be demoted by listing their attribute as a preference. In particular the height
failure is NOT reclassified. A future authoring pass needs a fresh plan if it
chooses less unnecessarily precise required designs. Semantic aliases or a false
claim that a detail is non-narrative cannot be disproved by schema validation;
the policy and plan still need content review before spending.

## Alternatives rejected / risks

No broad props-warning category, no post-failure prose regex, no global threshold
reduction, no deleting old findings, no repeated higher-quality retries. New
model output still has epistemic limitations: it can miss or misclassify pixels.
No visual accuracy or autonomous classification claim follows from unit tests.
Do not repurpose old calibration as certification of this opt-in policy.

## Validation / rollback / assignment

Mocked real caller path: cosmetic variation retained with no repair; every core
defect and uncertainty blocks; malformed/missing/wrong-policy evidence rejects;
old evidence cannot become new acceptance; predecessor uses identical policy;
repair includes only required defects. Test changed identities, stale assets,
input mutation, illegal non-sample use, existing behavior and blind-anatomy veto.
tsc and focused tests required. Full gate is already RED (39 timeouts + two
assertions in the recorded comparison run); do not relabel it or overlap gates.
Rollback: omit the new opt-in asset on fresh runs, or revert this milestone.
Old bound output directories may never be reused with new policy.

Cost allowance for this milestone: $0, zero images/audio/provider calls. Guy's
next visual decision follows independent QA and a fresh bounded sample, not this
code check. Claude should falsify every boundary above, especially aliases and
semantic misclassification, without self-PASS by Codex. No new product choice is
needed for this deliberately narrower implementation.
