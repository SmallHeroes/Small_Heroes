# Publish the product-accepted visual directions

Base: 4ab3bf1ce6f5ac0de42bb7dc7140867ed477d1d8. Start clean, local/upstream0/0.
Worktree C:/GNart/Work/sh-r3b1b-semantic-m1; branch codex/r3b1b-semantic-recovery-m1.
This task remains sole writer. Protected d53b768ccb2f and wave2 63ccb484 clean/read-only.

Guy explicitly answered "מאשר" to the exact twelve reviewed directions, including
page9 telescope correction, as the basis for continuation. acceptedAt is the UTC
recording observation, not a claim to the unavailable message-send timestamp.
Genuine Claude review is8d85366e..87e892c5, PASS0/0/2, candidate407c34c8/bundle6b125a90.

## Implementation and stop-check

Publish through existing visual-direction acceptance prepare/publish lifecycle.
Current obstacle is absent product acceptance, now supplied, not an engine defect.
This is story data lifecycle, not a story-specific code patch. No source/direction
rewrite, new creative decision, anchor/style change, threshold bypass or provider.
New immutable revision only; original accepted text and both candidates remain.
Local source selection may now resolve the integrated source where it previously
held; render eligibility must remain false until a fresh visual contract/package.
Test this boundary, not just file creation. Do not restore stale visual fallback.

Scope: inputs, real accepted revision, receipts/verifier/evidence and CURRENT.
Run prepare dry/write; publish dry/write; replay both with write:true and preserve
snapshots of generated artifacts. Validate real accepted authoring loader, source
bytes and reviewer identity. Run relevant lifecycle/authority/source tests and tsc;
assess inventory changes without relabeling unrelated failures as green.
No image/audio spending, credentials, runtime locator, database, deployment or push.

Rollback: do not select or consume this new immutable revision; any rollback of
selection must follow the existing lifecycle, not deleting evidence. No rollback
performed. Independent QA must attack approval binding, exact artifact reuse,
idempotence, source resolution and continued render exclusion. Product acceptance
of pixels remains future work. Next authoring must carry both accepted P2s into
actual contract state: one telescope custodian and clearly visible Adam at wheel9.
