# Panda canonical contract authoring — Decision Gate
Date: 2026-09-18. Base: 508f49b484a05e8f2cd0aac6e86706724da09b43.
Sole writer: Codex, current task, C:/GNart/Work/sh-r3b1b-semantic-m1,
codex/r3b1b-semantic-recovery-m1. Start clean, ahead1/behind0.
Protected dependencies: d53b at768ccb2f and wave2 at63ccb484, clean/read-only.

## Observation, cause, expected outcome
Claude supplied PASS0/0/0 new for4ab3bf1c..508f49b4. Source407c34c8 is
product accepted but has no fresh visual contract. This is an uncompleted
pipeline stage, not a new story defect. Author one candidate bound to the exact
accepted source, then audit semantics. No engine/prompt/threshold change.
Retain existing P2 obligations: Adam clearly holds wheel on9; exactly one
telescope custodian/resting place in each selected illustrated moment.

## Scope, order, validation
Continue in this task (no conflicting writer). One canonical live authoring
execution using scripts/visual-contract-authoring.cjs, not a bypass renderer.
Input snapshot36230817452cdbfd56896bd445c07a39b3b526cfe11475f3533235f2d0cb2143;
request1922c7d8a6bb8497e9a89f51eb6a261b30aa32703e912c7712ae680aaa4b0b62.
Factory-generated request validates with zero issues; import preflight passes.
New immutable outputs only; preserve source/candidate/approval/old logs.
Run existing artifact verifiers, inspect receipt/source bindings, replay offline,
audit actual returned contract and cost, tsc, git diff --check.
No independent PASS claimed for the new output. Claude should falsify exact
bindings, cost arithmetic, preservation, runtime boundary and both P2 obligations.

## Authorization, cost and alternatives
Guy repeatedly authorized autonomous continuation and reuse of the existing key.
This standing authority is separate from source acceptance and the readiness
audit (providerSpendAuthorized:false). No new product decision for exact directions.
Model gpt-5.6-sol, medium, default Standard tier. Official pricing checked:
https://developers.openai.com/api/docs/pricing — input4/cached0.4/output20 USD/M,
cache-write5; existing policy conservatively includes1.1 regional multiplier.
Reserved projected maximum7.656 USD; hard fence10 USD, not an actual charge.
At most7 standard calls plus1 strictly gated terminal-reference cleanup;
transport retries0, no fallback. No second blind live execution on failure.
Only the existing OPENAI_API_KEY from the main project env is injected in memory
into the canonical child; no key is printed, copied to a file, or committed.
Rejected: ad-hoc image render, manual contract authority fabrication, changing
approved prose/directions, replacing the current judge, or bypassing budget gates.

## Stop-check, rollback and exclusions
Story-specific input to the general existing engine, not story-specific engine code.
No other story/child/style behavior or production consumer changes. Smallest useful
run is one contract, zero images/audio. No new Cowork creative review necessary:
Guy accepted these exact directions. Future pixels still require product assessment.
Candidate is held for reconciliation/review; it is not approved by this execution.
Rollback: leave isolated outputs inert and revert only the evidence commit if needed;
never delete historical artifacts or pretend incurred spend can be rolled back.
No publication/Blueprint/Board/package/render/narration/deployment/push here.
Output roots are ignored by Git; no verified off-machine backup. Preserve execution
logs locally and track a bounded evidence inventory and handoff.
