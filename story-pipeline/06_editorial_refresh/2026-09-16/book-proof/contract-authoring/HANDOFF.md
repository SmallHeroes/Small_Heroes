# Panda paid contract authoring: complete execution, semantic HOLD
Date2026-09-18 (Asia/Jerusalem). Evidence milestone, not independent PASS.

## Original requirement and topology
Continue accepted Panda through our system toward a complete illustrated/narrated book;
reuse existing key under Guy's standing explicit authorization. Preserve exact
accepted source and directions. No ad-hoc renderer or paper-only readiness claim.
Branch codex/r3b1b-semantic-recovery-m1, worktree C:/GNart/Work/sh-r3b1b-semantic-m1.
Sole writer Codex current task. Review base508f49b484a05e8f2cd0aac6e86706724da09b43;
review head is the focused commit containing this HANDOFF (resolve git log -1 -- this file).
Started clean ahead1/behind0; no push this milestone. Protected dependencies
d53b768ccb2f and accepted-intent-wave2 63ccb484 clean/read-only.
Claude supplied PASS0/0/0 new for4ab3bf1c..508f49b4. It does not cover this paid run.
Prior P2 custody/staging obligations remain, not silently closed.

## Execution, exact result and money
Canonical CJS launcher, gpt-5.6-sol medium/default, unchanged policyv22/requestv56.
One run2026-09-17T21:37:49.158Z..21:41:30.441Z, native exit0, stderr empty.
Request1922c7d8; snapshot36230817; receipt68352dc5; candidate artifact41d40069;
template0521e50b; replay evidence98b1de1b; readiness74ad8d28.
Full identities and raw SHA256/bytes are in verification.json and request.json.
Two provider calls: initial0.413512 USD and source_evidence_id_patch0.023932 USD.
Repair fixed2 malformed source-evidence ids on9/10; zero final structural issues.
Usage input16614 (cache-write16608, cached0), output17719 including2597 reasoning.
Nominal estimated cost0.437444 USD; conservative accounted0.481196 USD.
These are receipt-usage calculations, not a separately verified provider invoice.
Reservation7.656 and hard fence10 USD are NOT charges. No transport retry,
fallback, terminal cleanup, image, narration, second live run or push.

Candidate status remains candidate. BlueprintAuthoringReady:false,
d1a1Authorized:false; no reconciliation approval, effective authority or package.
Do NOT run run-once.cjs again: it explicitly refuses existing execution/output roots.
The credential was injected into the child in memory; no key is in evidence.

## Integrity and validation
- Focused3 specs196/196: canonical-live-authoring-boundary177,
  accepted-story-source-authoring-authority5, authoring-replay-evidence14;
  maxWorkers1. Tests run against unchanged production code.
- npx tsc --noEmit exit0; both existing acceptance/refinement verifiers exit0.
- verify.cjs recomputes6/6 content addresses,9/9 accepted source bytes/mtime,
  usage pricing and budget, replays exact captured sequence offline with
  providerCalls0 and every receipt congruence true, and confirms output preservation.
- semantic-witness.cjs independently reproduces the observations below from the
  immutable candidate + decoded captured draft + accepted directions.
  Its exit0 means the HOLD observations reproduced, NOT that content passes.
- No npm run check; no stability/release claim. No production code changed.

## Actual semantic findings; do not render this candidate
1. CAST: extractor humans=[] and template humanCast=[]; all12 cast lists omit
   Adam/station children/teacher even when prose requires them. Facts overlay is
   authoritative in compileBookVisualContractTemplate.ts around4467. This is not
   merely an LLM omission that repeated generation will reliably fix.
   Existing supportingCastReview and semanticCorrectionPreview are the intended
   source-bound overlay path; paid candidate factory explicitly forbids smuggling
   reviewed preview authority into a legacy paid candidate.
2. MOMENT: captured draft itself requests mutually separated story beats as must
   actions: page8 helping hand AND flattening; page12 walk/roll/place while selected
   moment is final conversation. Page5 telescope viewing competes with rear turn.
   Compiler projects these into mustShow. Valid typed coverage is not semantic fidelity.
3. COVER: draft mustShow wheel/station; compiler appends before-firstRevealPage
   prohibitions for cover0. Compare raw draft with compiled cover to prove attribution.
   Existing cover-visible-prop operation can resolve explicitly; do not drop global
   lifecycle policy or blindly infer blanket cover permission for every prop.
4. CUSTODY: telescope on6 'with the parked rover or Adam';11 'available but secondary';
   on4 'inside' a lifted floorless box has no support;3 can read as two tubes.
   Existing prior P2 not closed. Page9 wheel is explicitly held by Adam in propState,
   but Adam still lacks an authoritative cast identity.
5. STAGING:7 sits_on rug vs accepted grass beside it;10 lacks child's pointing
   discovery and asks turns. Preserve the accepted selected moment.
6. CAPACITY follow-up: scale says several children; approved rover moment has child
   and Adam inside, companion outside and station driver following, not inside.

No new defect counts are attributed to Claude. These are Codex observations,
including physical ambiguity, not pixel evidence. See semantic-observations.json.

## Next bounded milestone (no fresh live authoring)
Prepare source-bound supporting-cast review from exact accepted story/directions;
use existing offline semantic-correction operations to select coherent single
moments and explicit custody. Preserve original6 artifacts and all execution logs.
Inspect the correction schema before editing: if it cannot express the necessary
change, design/test a general extension in a separate code milestone, not fake a
permitted operation or rewrite the paid artifact. Cover visibility needs an exact
bounded choice compatible with accepted source. Then independent semantic review,
reconciliation/consumer advancement and only then Blueprint/references/LOW sample.
No new exact-story approval is requested. Product acceptance of future pixels is separate.

## Storage limitation
ALL actual runtime artifacts and original execution logs remain ignored/untracked
under outputs/ on this machine; no off-machine backup is verified. Git/push of this
evidence commit does not preserve them. Original execution logs cannot be recovered
merely by regenerating. This commit tracks request, checks, hashes and observations,
not the large candidate/replay corpus. Existing source snapshot is reconstructible,
but reconstruction is not preservation of original log timestamps.
Roots needed for this review:
- outputs/panda-contract-authoring-20260918-01/ (6 content-addressed files)
- outputs/panda-contract-execution-20260918-01/ (6 original log/snapshot files)
- outputs/panda-contract-source-20260917/ (prior source snapshot input)
Prior acceptance/refinement roots remain as enumerated in their handoffs.
verification.json inventories all12 files in the two new roots.

## Independent QA: read-only falsification targets
Pin base508f49b4..the containing commit. Reconcile topology, no credential/provider access.
Check execution existed and native exit0, exact byte/digest/source binding, independent
cost math (reasoning included in output, cache-write price5, reservation not spend),
offline replay and9 source mtimes. Decode responseJson (it is a JSON string) before
comparing draft vs compiled output. Try to disprove every semantic observation and
separate model-authored issues from deterministic projection. Do not award render
readiness merely because replay/structural checks pass. Confirm no code/source,
reviewer verdict, candidate, price policy, thresholds or runtime locator changed.
Do not invoke run-once.cjs. Test offline commands below; no full gate was claimed.

## PowerShell handoff
Run from this exact worktree. Commit is already made at delivery; no staging needed.
```powershell
Set-Location 'C:/GNart/Work/sh-r3b1b-semantic-m1'
git status --short --branch
git log -2 --oneline
git diff --stat 508f49b4 HEAD
git diff --check 508f49b4 HEAD
node --import tsx --require ./scripts/shims/register-server-only.cjs story-pipeline/06_editorial_refresh/2026-09-16/book-proof/contract-authoring/verify.cjs
node --import tsx --require ./scripts/shims/register-server-only.cjs story-pipeline/06_editorial_refresh/2026-09-16/book-proof/contract-authoring/semantic-witness.cjs
npx tsc --noEmit
# Only after Guy explicitly authorizes a push:
git push origin codex/r3b1b-semantic-recovery-m1
```
