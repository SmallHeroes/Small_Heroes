# Five consecutive owner-requested sample pages

Date: 2026-09-19. Sole writer: current Codex task. Worktree:
`C:/GNart/Work/sh-r3b1b-semantic-m1`, branch
`codex/r3b1b-semantic-recovery-m1`, starting HEAD `32654edf`, clean/ahead5.
Protected d53b/accepted-intent worktrees remain clean at 768ccb2f/63ccb484.

## Requirement and owner decision

Guy explicitly asks for five consecutive pages, authorizes the existing paid key,
and asks Codex to continue without more approval prompts. Select Panda pages1-5,
not the cover. Accepted prose/directions revision407c34c8 remains byte-unchanged.
Latest supplied independent PASS is exactly 3ae68d60..32654edf, 280/280, P0/P1/P2=0.

## Observation, cause, solution and scope

The canonical paid candidate41d40069 remains semantically HELD. That chain still
requires supporting-cast, moment, cover, staging and custody recovery. This task
does NOT relabel it, self-approve it, or feed it to a renderer. Completing that
chain is not demonstrated by the sample below.

The existing `run-owner-book-draft --sample` is a separate, explicit local
owner-requested unaccepted-draft lane, already using the shared image adapter,
quality loop, usage checkpoints and stop-before-next-page behavior. It caps the
sample at3. Extend the bounded sample to5; keep at most3 prior comparison images
so the judge's six-reference ceiling remains unchanged. Version only the newly
supported4/5-page identity; old1-3-page run identities retain their bytes.

A NEW complete draft render plan is authored from exact accepted directions,
not copied from the held candidate or obsolete musical Panda book. It explicitly
assigns supporting cast, fixed playground geography, one selected instant, prop
custody and bottomless-box capacity. It is a diagnostic plan, NOT a Blueprint,
semantic correction, accepted visual package or source publication. Accepted
direction text is retained in the plan scene; checks verify bindings and selected
moments. Old artifacts and source files are immutable.

This is an explicit scope distinction communicated to Guy, not a claim that the
canonical HOLD is fixed. It gives the requested visual evidence through an existing
repository lane while production qualification remains separate.

## Files, order and validation

1. Extend sample selection in scripts/run-owner-book-draft.ts and its spec.
2. Validate5-page sequencing, stop behavior, bounded prior-image context, budget
   reservation, preserved small-sample identities and rejected6-page samples.
3. Prepare new bound plan/config plus read-only verification. Run tsc, focused
   specs and full check without overlapping test runs. No stability self-PASS.
4. Commit focused implementation. Run preflight before credentials/provider calls.
5. Execute selected pages through the same shared QA loop, inspect outputs,
   preserve original/repaired images and all receipts; show honest result/cost.

## Cost and safety

gpt-image-2 LOW,1024x1536. Expected1 prop board+5 page generations; at most1
bound-defect repair in this sample. Image fence$3.50; QA fence$6.00; aggregate
conservative admission ceiling$9.50. Existing judge gpt-5.5 medium/flex unchanged.
Reservations are not invoice charges. Unknown paid outcomes stop, never blind retry.
No audio requested for this five-page task; no full book, production, deploy or push.
If the shared gate holds a page, diagnose before any further paid action; never
replace its verdict with manual PASS or render later pages by disabling the gate.

## Stop-check / risks / rejected alternatives

General code change, story-specific data, no production consumer or thresholds
changed. Existing key is explicitly authorized; no credential values in artifacts.
Do not render the held candidate, use an ad-hoc image API runner, split into batches
to evade the sample cap, or silently skip per-page QA. Pixel compliance and judge
accuracy remain unproven until observed. Latest3 comparison images bound cost;
older image comparisons are not claimed, canonical design locks remain supplied.

## Rollback / review

Revert the focused code commit to remove4/5-page support; preserve all paid evidence.
Claude should attack6-page rejection, stop-on-hold, reservation math, reference cap,
legacy identity compatibility and plan/source binding. No independent PASS is
self-awarded. Guy can judge framing, expressive acting, prop/character continuity
and actual pages; sample acceptance does not publish the book or close canonical HOLD.
