# Selected-prop sample follow-up

Guy authorizes completion of five consecutive paid sample pages. Sole writer/current
task: C:/GNart/Work/sh-r3b1b-semantic-m1, codex/r3b1b-semantic-recovery-m1.
Base5d8de889. No source/canonical candidate/production authority changes or push.

## Observed failure and root-cause scope

Original paid run outputs/panda-five-page-sample-20260919 retained:
board3ae7ec1c, page1 73140760, repair752d0bb1, page2 167a529d.
Station window occupies front wall in both page1 attempts despite required left
side wall. QA flags original, falsely passes repair. Page2 shows future props.
Full prop board contains these props and is supplied on every page; full plan
likewise reaches contextual QA. These are demonstrated input leaks, NOT causal
proof that removing them guarantees correct pixels or accurate judgment.

Operator stopped exact matching child process; native exit4294967295, wrapper1.
No sample manifest was produced. Logs in panda-five-page-execution-20260919,
inputs in panda-five-page-input-20260919. Preserve run.lock and unknown page2
contextual claim; do not resume or retry. Five known QA receipts plus one unknown.
Images accounted upper$0.5265; QA$2.57386 including unknown reserve$1.
False PASS remains recorded separately from operator HOLD, never overwritten.

## General implementation and owner scope

Optional hash-bound board atlas coordinates, exact prop-ID inventory, dimensions
validated before credentials. Mechanical crops of original board, no new designs.
Each selected page receives only its selected atlas regions plus child/companion;
same refs go to judge. Rectangular crops may include incidental edge/background
pixels, so this is not segmentation and does not guarantee absence of hallucination.
QA receives selected page/active designs/effective continuity, not future pages.
At most3 prior comparison images/context; no judge/model/threshold changes.
Policyv5 only on opt-in projection; old paths byte-compatible in identity.

New camera clarification retains existing left-window/front-door topology and
source main actions. No weakening to match rejected pixels. New plan53463fe7.
prepare-selected-props.cjs reproduces input/atlas files; first local preparation
failed an overly tight 10k context guard AFTER files were written. No provider
called. Guard moved before writes and corrected to12k (observed max10915); rerun
asserted byte-identical existing files and succeeded. Provider cap remains50k.

New input/output/execution roots use panda-five-page-selected-{input,sample,execution}-20260919.
Existing key explicitly authorized. Images LOW, max5 additional, board reused.
No additional repairs/initial rejected images. Old$3.10036 + new image fence$2.50
+new QA fence$3.89964 = $9.50; unknown reserve never released. Shared gate stops
before next page on hold. No ad-hoc API runner or production cutover.

## Validation and falsification targets

Focused143/143 across owner67, quality28, judge16, checkpoints32; tsc0 and
preflight0/providerCalls0. Full check final result in CURRENT. No self-awarded
independent PASS. No model accuracy claimed by these tests.

Attack: out-of-bounds/missing/unbound coordinates before key access; selected
pixels actually exclude unrelated atlas regions; no future context/props; same
refs in renderer and QA; preserved old identity versions; sample stops on hold;
aggregate budget keeps interrupted reservation; original artifacts unchanged.

Rollback: revert this focused optional code change, preserve all paid artifacts.
Canonical semantic HOLD still open; sample is local unaccepted editorial lane,
not Blueprint qualification, customer readiness, or render of candidate41d40069.
Claude first review read-only. Guy retains product acceptance.

```powershell
Set-Location 'C:\GNart\Work\sh-r3b1b-semantic-m1'
git status --short --branch
git log -3 --oneline
git diff --stat 5d8de889 HEAD
git diff --check 5d8de889 HEAD
# Only after Guy explicitly requests propagation:
git push origin codex/r3b1b-semantic-recovery-m1
```
