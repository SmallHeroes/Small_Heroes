# Claude re-gate: whole-book planner and repair capacity

First pass READ ONLY. No provider, credentials, images, publication or push.
Original owner requirement: whole story/continuity planned before images, without
freezing camera/composition/acting. Codex implements, Claude independently reviews.

## Freeze scope first

Worktree C:/GNart/Work/sh-r3b1b-semantic-m1; branch codex/r3b1b-semantic-recovery-m1.
Base5e9908815cbf97816a28923cf8b99c58684e0f69, initially clean/ahead10. Head is the
single corrective commit containing this file; resolve it and freeze before review.
Same task is sole writer; dependencies768ccb2f/63ccb484 are clean/read-only.
Prior verdict: HOLD P0=0/P1=1/P2=1 for093d37c0..5e990881, not PASS. Its207 tests,
both witnesses and log byte identities were independently reproduced. No full check
or live joint-planner capacity measured by Claude.093d37c0 has no independent PASS;
validateBookSequence remains a dependency to review, not silently approved here.

## P1 response

Valid risk:14000 included reasoning AND larger joint output. Now output policy is
16000+1000*bodyPages:8=24000,12=28000,16=32000, within documented GPT-5.4 maximum.
Model/medium unchanged. No statement that higher cap guarantees any model completes.
The real request, not merely a compiler fixture, is exercised at8/12/16 pages.

Capacity derives input upper allowance from UTF8 JSON of instructions/input/schema
plus4096 protocol allowance, rejects>90000, then reserves ceil-cent((input+output)*
30/1M), same conservative rate as previewUsageUpperUsd. The actual request/claim and
run identity bind policy, input allowance, output cap and reserve. No guess that a
4.51 chars/token sample is a universal tokenizer. Reservations are upper estimates,
NOT invoices. Existing10USD run fence/unknown claim handling remain in force.

The upfront floor is max(old nominal image floor, planner reserve+0.50). It does not
sum mutually sequential maximum reservations or promise full-book completion. Each
image/QA step still gates actual remaining funds. This avoids falsely declaring all
16-page books impossible merely by summing maxima, without increasing the run budget.

Incomplete status + incomplete_details are retained BEFORE validation; explicit
planner_incomplete_new_run_required. Replay repeats the hold without another call.
Recovery is NOT automatic: investigate the reason, deliberately use a NEW root under
the appropriate policy/budget; old result/claim/charge stays. A new root is a NEW paid
attempt, never a free replay. No retry/migration command executed this milestone.

## P2 response

Initial24000 cap remains. Repair is separate compact effective-state projection with
31000 local limit, still checked by real planGPTImageRequest against32000 including
prefix and multipart CRLF. No global image limit/transport/model change.

All page directions, wardrobe/style, active prop/location designs, scale/framing,
all CURRENT entity attribute values and complete sequence packet remain. Historical
invariant duplicates are omitted after projection, not deleted from evidence. Every
defect correction remains complete (whitespace normalized, no substring truncation).
Verbose observations are diagnostic evidence, NOT copied to this edit instruction;
raw judge records/history retain them. This is a projection tradeoff, not a claim
that all QA prose survives. If a correction is vague and its observation carries
essential localization, pixel outcome remains an empirical risk. No QA weakening.

preflightLocalBookPrompts runs BEFORE prop board/cover/page image. Every page must
fit both initial and ALL8 schema-max1800-character corrections. The correction bound
comes from the actual schema. No fictional reviewed predecessor is manufactured;
preflight uses null +512-character metadata margin, actual execution still requires
hash/review-bound predecessor. Real-entry tests verify the late-page failure blocks
the FIRST board/image, while a max8-defect repair actually reaches mocked dispatch.

sequencePageState extraction does not change packet key order/content or validator.
Owner-draft uses the same existing packet and is otherwise untouched; its separate
repair capacity limitation is NOT closed by this local-preview correction.

## Evidence and tests

Focused216/216: planning39, sequence29, owner72, quality28, judge16, preview32. tsc0.
One development assertion mismatch: newline boundary correctly threw the provider
limit error; my test expected the wrong message spelling. Corrected test, no waiver.

Read-only verify.cjs uses real saved Dini usage:8819 output,5091 reasoning,3728 visible,
16813 chars. Our explicitly minified sparse Panda projection is11293 chars, different
from Claude's12683 serialization. Estimated joint12=11323 and16=13400 tokens assume
unchanged reasoning and the same ratio; NOT measurements. Capacity decision does not
depend on that ratio being exact. No paid output or usage record is fabricated.

Same witness runs actual new preflight on saved Panda plan/sequence:13 pages including
cover, max initial23332 and max repair30452 multipart characters including512 metadata
margin and ALL8 max corrections. This uses local-preview's actual builder, NOT the
owner builder's22296 as a substitute. Input bytes+mtimes are unchanged.
Prior preservation witness still0:56 historic snapshots,3 reader hashes,2 images.

Final npm run check native exit0, both typechecks0. Ordinary391 files:5810 passed,
73 skipped, exit0, elapsed133108ms. Resource22 files:671 passed, exit0, elapsed204324ms.
Total6481 passed/73 skipped/0 failed,413 inventoried files (396 passed/17 skipped).
Serial phases with no overlapping focused suite. One implementer-observed green
run, not independent QA, enduring reliability or release acceptance.

Raw log SHA-256:
- check.stdout.log,219133 bytes:
  1846b96f0d0ee8b48ffa3fa69e7e9e429725668102fc55118f7eba4ce0f1a417
- check.stderr.log,195975 bytes:
  e49f5508b3b5587740df0b9698b0d9d09a6da32e26a49b08218329775902dcd3
stderr is retained, including expected negative-test output and phase diagnostics.

## Attack targets

1. Inspect actual responses.create max_output_tokens and plan.claim reservation for
   8/12/16, poison request size/budget, prove no schema/input omission in admission.
2. Replay incomplete twice, retain status/reason/usage, zero second dispatch. Try a
   historical v1 identity and changed capacity; no root migration or quiet overwrite.
3. Largest last-page context must reject BEFORE any board, even if first page fits.
4. All8 longest corrections, Unicode/newlines, full state after attribute changes,
   max predecessor metadata, four-reference cap and own-candidate repair target.
5. Attack projection: current-state values survive, original evidence remains, packet
   still identical in QA/repair, camera/acting untouched. Observe the explicit omission
   of diagnostics; do not mistake this for a semantic/visual repair guarantee.
6. No source/acceptance/QA-calibration/production runtime mutations; scoped gate, not
   all18 model-generated plans. Full prop-board pixels remain an acknowledged limit.

## Storage, commands and owner handoff

New logs at outputs/whole-book-capacity-validation-20260919/{check.stdout.log,
check.stderr.log}: ignored/local only, no verified off-machine backup. Old Dini/Panda
roots remain local per prior disclosures. verify.cjs writes0. Test temporary roots
cleaned. Cost0, no provider dispatches, credential reads, new book or product acceptance.
The prior unknown1USD reservation remains untouched. No independent self-PASS.

```powershell
Set-Location 'C:\GNart\Work\sh-r3b1b-semantic-m1'
git status --short --branch
git log --oneline 5e9908815cbf97816a28923cf8b99c58684e0f69..HEAD
git diff --check 5e9908815cbf97816a28923cf8b99c58684e0f69..HEAD
npx.cmd vitest run lib/__tests__/local-book-planning.spec.ts lib/__tests__/local-book-sequence.spec.ts lib/owner-book-draft.spec.ts lib/__tests__/local-preview-quality.spec.ts lib/__tests__/local-preview-judge.spec.ts lib/__tests__/local-story-preview.spec.ts --silent
npx.cmd tsc --noEmit
node --import tsx --require ./scripts/shims/register-server-only.cjs story-pipeline/06_editorial_refresh/2026-09-16/book-proof/capacity-correction/verify.cjs
node --import tsx --require ./scripts/shims/register-server-only.cjs story-pipeline/06_editorial_refresh/2026-09-16/book-proof/sequence-recovery/verify.cjs
```

Codex commits locally; no need to stage/commit again. Push is not authorized by this
brief and would carry ALL ahead commits, not only the correction.

```powershell
Set-Location 'C:\GNart\Work\sh-r3b1b-semantic-m1'
git status --short --branch
git log --oneline '@{upstream}..HEAD'
# Only after Guy explicitly asks to push:
git push origin HEAD:refs/heads/codex/r3b1b-semantic-recovery-m1
```

Official references checked2026-09-19:
[GPT-5.4](https://developers.openai.com/api/docs/models/gpt-5.4),
[image edit limit](https://developers.openai.com/api/reference/resources/images/methods/edit).
The existing transport guard already encoded32000; no speculative override added.
