# General first creative replacement from original accepted sources

## Intent, authority and topology

Guy's active goal remains a good complete book through the real system, prudent
spend, general fixes. Previous turn was progress: two commits, real Editor pass,
full check6342pass/73skip. This is not completion or a blocked turn.
Continue same task, sole writer, C:/GNart/Work/sh-r3b1b-semantic-m1,
codex/r3b1b-semantic-recovery-m1, clean4fd7eda0 ahead54/behind0.
Protected d53b768ccb2f and wave2 63ccb484 stay read-only. No new task or push.

## Evidence and cause

All18 catalog roots are accepted-source/v1,16 lack revisions. Creative
replacement accepts only revision predecessors and requires revisions/ to exist.
Original manifests have no revisionDigest. They bind exact story, Editor review
and either a single-story acceptance (1) or corpus acceptance/record (17).
The existing single-story validator and corpus promotion validator can validate
those approval shapes; neither existing minimal root loader proves the full
chain. Current source is never inferred from the new prose's Editor pass.

## Change and compatibility

Add a contained original-source verifier reusing existing approval, editorial
and source validators. Verify exact manifest shape, all local bound bytes,
slot/identity, story/review/approval hashes, audit, exclusions and corpus member.
An old ignored raw single-story review path is historical provenance, not a new
required external file: accepted review bytes and approval bind that exact review.

Add explicit request/v2 for an accepted_root_v1 predecessor (manifest path/hash,
no invented revision digest). Give its derived identity/review/acceptance/manifest
artifacts v2 versions; preserve existing request/v1 and produced bytes unchanged.
Shared reload and enrichment consumers must handle the new result, not just an
unused adapter. Fresh text acceptance is still required; no approval is inherited.
Dry-run permits absent revisions/ but writes nothing. Actual first publication
creates it only after validation and under an exclusive lock. Refuse stale root
forks, aliases, held locks and collisions; preserve exact idempotent replay.

## Validation, risks, rollback

Red test against current implementation, then positive single/corpus fixtures,
all18 real roots read-only, byte/hash/identity/approval/corpus tampering, traversal
and links, missing revisions, fork/replay/lock controls, unchanged legacy tests,
real downstream consumer preparation, tsc and full check. No threshold, timeout
or test exclusion change. Trust rests on repository-owned approvals; mutually
forged source+approval cannot be defeated merely with hashes. No self-QA PASS.
Rollback code separately, retain evidence. No actual accepted source is changed.

## Stop-check and non-goals

General source transition, no story-specific bypass, no provider, cost, image,
audio, old-direction import, visual/runtime locator or production cutover.
No fresh Guy content acceptance is invented. Current authorization covers this
engineering step, not release. Existing independent QA gates remain separate.
Rejected: transplanting old directions to mint an intermediate revision, changing
the accepted root in place, treating a manifest hash as a revision digest, or
rendering in a side lane to hide a missing pipeline connection. Claude should
attack identity/approval substitution, absent-folder preview writes, stale roots,
reload fidelity and propagation into the genuine enrichment consumer.
