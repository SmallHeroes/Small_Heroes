# Book proof: repair two deterministic test premises

## Scope and authority

Continue the current sole-writer task in C:/GNart/Work/sh-r3b1b-semantic-m1,
codex/r3b1b-semantic-recovery-m1, clean 08eb84b2 ahead52/behind0.
Protected d53b768ccb2f and wave2 63ccb484 are clean and read-only.
Guy's ongoing authorization covers this narrow general engineering correction.
No new product decision, spend, provider/model change, render or deployment.

## Observed, expected and cause

The full check failed two assertions; isolated replay also fails exactly those
two (7 pass, 2 fail). Discovery reports410 specs while a test pins393. The test
should require exact coverage and a disjoint policy partition, not freeze the
number of independently added ordinary specs. Keep the22 resource targets and
all named inclusion/negative assertions; add a genuine new-spec control.

The model scan captures claude-4 from the existing Replicate registry identifier
anthropic/claude-4.5-sonnet. Its real caller dispatches to api.replicate.com, not
the first-party Anthropic API. Preserve full lexical identifiers; permit only
this existing qualified identifier at its exact registry path, with the existing
version pinned separately. Unknown qualified and bare identifiers still fail.
This records existing configuration, not a vendor availability or pricing claim.

## Plan, risks and acceptance

Change only the two specs and engineering records. Do not modify runtime code,
authority constants, policies, timeouts, test exclusions or accepted manuscripts.
Add negative controls against truncation, arbitrary namespaces and path leakage.
Then focused tests, tsc and full npm run check. A full green run is an observation,
not proof of permanent load stability or visual quality. No inherited-failure
claim without a baseline comparison. No independent self-PASS.

Rejected: merely updating393 to410 (next spec breaks again), allowing claude-4
(fictional truncated token), ignoring all namespaced IDs, removing assertions,
switching providers or increasing timeouts. Rollback only these test changes;
no data migration is needed. No image/audio/API costs in this milestone.

Stop-check: general test-contract correction, no story/child specialization,
production behavior unchanged. No Guy visual choice is needed. Claude should
try to omit/duplicate inventory, route an unknown model through a namespace,
move the exception to another file, or alter its pinned version. Editorial and
visual acceptance remain separate; nothing here authorizes source publication.
