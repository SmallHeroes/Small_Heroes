# עותקי קליטה של הסיפורים המאושרים

18 הסיפורים בתיקייה זו שומרים את הטקסט, הכותרות והפרמטרים שאישר גיא.
ההבדל מעותקי העריכה הוא הסרת שני שדות מצב מכותרת הקובץ ותקנון סיומי שורות.
מצב ההכנה והקישור לאישור נשמרים ב־manifest.json ולא בתוך טקסט הסיפור.

הקבצים עברו את בדיקת פורמט הטקסט הקיימת במסלול הפרסום.
זאת אינה בדיקת עריכה ספרותית ואינה פרסום למאגר הפעיל.
הם עדיין ללא הוראות איור חדשות ואישור עורך חיצוני.
קלוד אישר את הכנת הקליטה בטווח 7b21c36a..e7f1a023 בלבד, עם הערת P2 אחת.
זה אינו אישור עריכה ספרותית או אישור מוכנות לפרסום.
אין לייבא אישורי עריכה או הוראות איור מהסיפורים שהוחלפו.

מגבלת הראיה: בכל 18 הנוסחים ובקודמיהם שבמלאי הקריאה אין שורות הוראות איור.
לכן בדיקת הקורפוס אינה מוכיחה טיפול בהוראות קיימות או הפשטה שלהן.
בדיקות הזרקה סינתטיות נפרדות מוכיחות דחיית הוראה לא ריקה וקבלת שורה ריקה,
כולל הוראה נוספת שמסתתרת אחרי שורה ריקה. הכלי דוחה הוראות, לא מסיר אותן.
אישור התוכן בקובץ OWNER_APPROVAL.json הוא עוגן אמון מנוהל ב־Git;
בדיקת האשים אינה מגינה מפני שינוי משותף של הטקסט ושל רשומת האישור עצמה.

לבדיקה ללא כתיבה או קריאות ספק, משורש המאגר:

```powershell
node story-pipeline/06_editorial_refresh/2026-09-16/prepare-intake.cjs --check
```
