# Local continuity, bounded correction and audio — implementation handoff

## Requirement and authority
Guy requested general stable character scale, object/set identity across scene
transitions, environmental framing, anatomical QA and automatic bounded correction
during creation, with narration for the next audition. Latest explicit approval:
continue without repeated questions. No release, production cutover or push authority.

## Topology / review range
Worktree C:/GNart/Work/sh-r3b1b-semantic-m1; branch
codex/r3b1b-semantic-recovery-m1. Base4d3bfab0; head is the focused local commit
introducing this handoff (exact hash in Codex's final message). First QA read-only;
reconcile HEAD before judging. Both protected dependencies remain at768ccb2f and
63ccb484, clean. Existing owned preview/review/reader edits were included explicitly,
not swept together with unrelated work. No independent PASS or push.

## Changes and claims
- local-preview-quality: structured entity identity vs source-evidenced state,
  persistent visible location references, pose-independent scale ratio, frame
  occupancy quotas, complete quality categories and bounded two-repair loop.
- local-story-preview/run-local-story-preview: shared framing/anatomy locks and
  real CLI callback wiring; known calibration required before paid run; failed
  candidates don't become next-page references; image/context evidence is bound.
- local-preview-judge: separate blind anatomy observation plus contextual review,
  whole image/four deterministic detail crops; exact-byte snapshots, no SDK retry;
  raw receipts preserved before parsing, incomplete evidence fails closed.
- Budget settlement preserves usage and unknown reservations; known over-reservation
  result holds without rebilling. Contextual QA allowance raised after observed gap.
- local-book-review and dev audio GET: optional hash/text-bound MP3 delivery,
  containment and dev-only/hosted-denied policy; legacy no-audio manifest still works.
- Local narration uses shared existing voice registry and TTS preparation, one
  request per page/checkpoint, local-only storage and no hidden transport retry.
- Earlier owned fixes included: local text scroll, manual repair/review scripts,
  optional anatomy flag for that older reviewer. These do not self-certify old images.

## Empirical result — HOLD, not solved
Calibration roots under outputs/local-preview-quality-calibration[-v2|-v3|-v4]-20260915.
v1 matched2/4; v2 matched3/4; v3 known incomplete output; v4 matched3/4.
All variants missed the owner-rejected original anatomy. Count/bridge detection
improved; corrected anatomy control passed. Expected answers/case labels were not
sent to the judge. Current calibration gate rejects the real v4 report, preventing
a new image run. No new book images were generated. This is working routing and an
empirically unready judge, not successful autonomous book quality.

The structured planner has not yet been exercised live, nor has automatic image
repair run against the new policy. Exact source quotations bind proposed changes
but don't prove their semantic interpretation. Pixel scale/framing need additional
calibration; selected prior-image context is bounded, not a full-book reconciliation.
Numerical resemblance, final whole-book audit and production integration remain open.

## Audio and preservation
outputs/local-preview-narration-dini-20260915 has12 real MP3s, voice mom/eleven_v3.
13 images/texts identical to previous reader;12 MP3/text hashes verified; accepted
prose225f2b01 unchanged. Reader uses existing controls; page1 loads with HTTP200,
readyState4, duration36.08/currentTime28.87 then pauses; no browser error logs.
Voice/pronunciation quality awaits Guy's listening. All outputs are ignored/local
only and not preserved by Git push. Never substitute this preview for customer release.

## Cost evidence
17 OpenAI QA receipts,12 audio receipts, no unknown claims. Conservative all-token
upper estimates across QA roots total6.69603 USD, not provider charges. Final v4
upper estimate3.07839 exceeded that root's3.00 allowance after the final response;
the observed accounting gap and resulting guard are explicitly recorded in CURRENT.
Audio has6.00 reserved, not a verified invoice. No changes to old receipts.

## Validation / limitations
Final focused eight-spec run:171/171 passed in20.78s; npx tsc --noEmit exit0;
git diff --check exit0. Includes real judge-adapter fault injection, narration
checkpoint tests, local image/audio route tests, shared image cancellation,
bunny regressions and runtime-world-authority. No provider in these tests.
Full check exit1, ordinary5402pass/6fail/73skip, resource644pass/27fail; run overlapped
edits, so no final-tree full-check PASS. Log:
outputs/local-preview-quality-full-check-20260915.log. No stability closure.
Full log SHA256:6adef7ab595eaa6c108b4c94a499179ef70053ab00b32aea935a763d8b3ed112.

## Falsification targets
1. Stale image/context, missing or duplicate QA category, uncertain/transport failure
   cannot pass or spend image-repair budget. Two repairs maximum; every retry rejudged.
2. Raw blind anatomy defect cannot be erased by contextual PASS. Both records retained.
3. Actual bad/control images, not mocks: prove the documented remaining anatomy miss.
4. State transitions: count/material drift vs legitimate slice/costume/place changes;
   cover chronology, background locations, unseen objects, supporting children.
5. Audio traversal/symlink, changed text/hash, hosted/prod access; identity/score/owner
   preservation; no hidden retries or token-based settlement of character-priced audio.
6. Call reservation underestimate: receipt preserved, explicit hold, zero rebilling.
7. Confirm shared legacy callers retain behavior and no local evidence becomes runtime
   approval. Do not confuse tests of routing with a calibrated visual judge.

## Copy-ready inspection / optional push
Only run the push line when Guy asks to propagate. This task did not push.

```powershell
Set-Location C:\GNart\Work\sh-r3b1b-semantic-m1
git status --short --branch
git log -1 --format='%H %s'
git diff --stat 4d3bfab0..HEAD
git diff --check 4d3bfab0..HEAD
# On explicit propagation request only:
git push origin codex/r3b1b-semantic-recovery-m1
```
