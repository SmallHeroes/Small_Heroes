# R1D General Story Source Visual Directions Enrichment — Decision Gate

**Date:** 2026-08-23

**Status:** APPROVED BY GUY through the authorized general creative-replacement route; no render authority

**Branch:** `codex/qa-wizard-presentation-dispositions`

**Worktree:** `C:\GNart\Work\sh-wt-r1d-output-budget`

## 1. Proposed change

Add one general, provider-free enrichment lifecycle that prepares exact Visual
Directions for an immutable, product-accepted `story_text_only` revision. The
lifecycle binds the source revision, one complete typed page-direction record,
its deterministic `integrated.md` projection and a review bundle into one
content-addressed candidate. It does not publish or activate the enriched
revision before independent technical review and Guy's exact artifact
acceptance.

The first candidate is for accepted revision
`eca8b3c8a8ed32a6a884cd9bd4fc493fcc6f00fed3c4ebe710c6a870ead2115d`
of `chameleon_koko_bedtime`.

## 2. Why now?

The story rewrite is accepted, but its manifest intentionally reports
`visual_directions_not_approved`. The existing direction wave reads only the
18 legacy top-level accepted sources, not nested creative-replacement
revisions. The existing accepted-revision lifecycle and downstream package
loader are also specialized to the historical `female -> neutral` metadata
migration. Reusing either route would misbind the old bus-stop story or falsely
label a full rewrite as a metadata correction.

Fresh source-bound Visual Directions are the next blocking authority before a
new Visual Contract, Blueprint, package and Wizard locator can exist.

## 3. Scope

- General system lifecycle plus one story-specific candidate data instance.
- Exact validation of the accepted story-only revision and typed direction
  record.
- Deterministic injection and byte-exact source projection.
- A closed composition review policy for eight-or-more-page candidates.
- Digest-bound review evidence and no-write/write/replay behavior beneath
  ignored `outputs/`.
- No accepted-revision publication in this milestone.

## 4. Risk of hardcoding

Production code must not mention Bar, Kim, Chameleon, kindergarten, lantern,
bedtime, a page number or a pinned digest. The general lifecycle reads all
story identity from validated inputs. The first candidate may contain its
story-specific visible facts, but critical continuity remains structured:

- child wardrobe is owned by frozen Visual Contract wardrobe authority;
- the final pajama change requires a later typed page wardrobe override;
- Kim's hue, pattern and body language are owned by frozen Companion State;
- Kim's identity and mustard satchel are invariant companion authority.

The direction prose must not compete with those authorities.

## 5. Files likely affected

- `scripts/story-source-visual-direction-enrichment-lifecycle.cjs`
- `lib/__tests__/story-source-visual-direction-enrichment-lifecycle.spec.ts`
- one ignored request/candidate root below `outputs/`
- `CURRENT.md`
- this Decision Gate and its implementation-evidence document

The predecessor revision, current package/locator, accepted Boards, catalog,
Wizard, existing revision lifecycles and generated images are preservation
surfaces.

## 6. Expected behavior after change

- Preview validates every input and derives the candidate without writing.
- Write atomically creates one exact candidate inventory beneath `outputs/`.
- Identical replay returns `created:false`; unlike bytes or extra/missing files
  fail closed.
- The accepted source story remains byte-identical after removing injected
  `imageDirection:` lines from `integrated.md`.
- Every story page has exactly one valid direction with accurate cast presence,
  visible action, setting identity and recurring-object continuity.
- For an eight-page candidate, the review policy requires at least one
  wide/extreme-wide frame, one close/medium-close/detail frame, four distinct
  shot types, three camera angles and no adjacent repeated shot/angle pair.
- The first candidate creates meaningful scale and angle changes around the
  chase, failed ring, discovery, child-led climax and quiet bedroom landing.
- Direction text defines no child appearance/wardrobe and no companion
  hue/pattern/body-language state.
- The candidate grants no Visual Contract, package, Wizard or render authority.

## 7. Validation plan

- Hermetic preview, write, replay, collision and hostile path/link/inventory
  tests.
- Reject stale source descriptors, story-key/page mismatches, incomplete page
  coverage, direction prose that claims protected wardrobe/companion appearance
  authority, and insufficient composition diversity.
- Prove exact story projection and deterministic integrated bytes.
- Materialize the real eight-page candidate at `$0` and inspect every page.
- Preserve predecessor, current locator and all four pre-existing untracked
  Board artifacts by SHA-256.
- Run the dedicated and adjacent direction/creative-replacement suites,
  `npx --no-install tsc --noEmit`, `git diff --check` and the proportionate
  repository check.
- Commit locally and give Guy a digest-bound Claude Code adversarial brief.

## 8. Cost impact

`$0`. No provider, model, image, audio, Vision, database, storage, deployment,
payment, network or render call is authorized.

## 9. Rollback plan

Revert the focused tracked commit and remove only the named ignored candidate
directory. The accepted story-only revision and current Wizard/package locator
remain unchanged, so rollback requires no runtime migration.

## 10. Review assignment

Guy already authorized the general creative-replacement route and explicitly
withheld render authority. He also fixed the product requirements: materially
varied framing with close views, stable non-gender-coded clothing until an
explicit bedtime pajama transition, Kim's invariant mustard satchel, and
gradual typed companion appearance states.

Claude Code must independently falsify source/path/digest binding, exact page
projection, composition-policy enforcement, protected-authority separation,
atomicity/replay/collision behavior, generality and preservation claims.

Guy must later accept the exact Visual Directions Candidate and Review digest
before publication. The first rendered pages remain the product eyeball gate;
technical validation cannot establish visual quality.

## Stop-check

1. This is a general lifecycle with one story-specific data instance.
2. It cannot change another story because no accepted or current locator moves.
3. It affects no production behavior in this milestone.
4. It spends `$0`.
5. The smallest proof is the eight-page JSON/integrated-source candidate; no
   image is required.
6. Guy has already decided the visual continuity/composition behavior and must
   later approve the exact artifact digests.
7. Claude Code receives the falsification targets above.
8. Claude Cowork is not required for the engineering lifecycle; the accepted
   story and product direction already passed their separate gates.
9. Guy will eyeball the later smallest approved LOW render before any full-book
   replacement render.

## 11. Do not do

- Do not mutate accepted revision `eca8b3c8...2115d` or its predecessor.
- Do not copy the old bus-stop directions.
- Do not encode wardrobe, child identity or Kim's appearance states as loose
  direction prose.
- Do not publish an accepted integrated revision before exact Guy approval.
- Do not create or migrate a Visual Contract, Blueprint, Visual Package or
  locator in this milestone.
- Do not change the Wizard, deploy, call a provider or render.
- Do not touch or stage the four pre-existing untracked Board artifacts.
