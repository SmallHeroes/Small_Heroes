# Decision: frozen-rubric expansion, no automatic render cutover

Guy approved continuing the automatic-QA investigation. Codex remains sole writer
in C:/GNart/Work/sh-r3b1b-semantic-m1, codex/r3b1b-semantic-recovery-m1,
base936ce9709e5ca05a5b07a5171d732abc718a52a6, clean ahead23/behind0 at start.
d53b768ccb2f and accepted-intent63ccb484 clean/read-only. Same task, no delegation.

Observed: explicit text rubric distinguished original/corrected Dini anatomy while
baseline and two visual examples over-rejected. Four targets/one defect are not
enough for automatic repair authority. Need frozen-prompt generalization evidence.
Contributing issue: prior runner is fixed to one story and conflates its own sample
selection with evaluation. No proven new defect detector architecture yet.

Implement a reusable local manifest runner, using existing comparison transport,
without editing the calibrated prompt, previous runner, active gate or image flow.
Manifest pins exact input hashes, prompt hash, labels, cohort and diagnostic basis.
All labels except original Guy rejection remain implementer-provisional. Source
roots are local outputs and explicitly scoped archived project outputs; no photos,
credentials or source-file contents enter requests. Provider sees only frozen rubric
and a single target PNG, never labels, filenames, test IDs or expected outcomes.

Smallest useful run:12 assessments;10 additional images and the2 original/corrected
replication controls. Different children/companions, kneeling, hands on hips, folded
hands, grip, mirror and occlusion. Main visually inspected every added target.
No additional unequivocal anatomical defect found in the scoped archive scan; this
is primarily a false-alarm/generalization probe, NOT multi-defect sensitivity proof.
Do not relabel a superseded file as defective just because it was superseded.

Up to12 calls,0.25USD retained reservation each,total3USD planning allowance (not
hard provider billing cap),3000 output tokens and120s cancel request per call.
Existing Replicate key only. No render/audio/edit/publication/upload URL or push.
Exact-byte replay, no automatic POST retry; malformed/transport outcomes UNKNOWN,
not visual defects and not image-repair authority. Keep old results immutable.

Validation: strict schema/count/duplicate/prompt/hash/PNG/path containment before
provider calls; manifests can't override prompt or limits; environment gating,
cache replay and run identity/lock. Focused tests + tsc + diff-check; full repository
check remains NON-GREEN, not claimed closed by this isolated tooling milestone.
Rollback: stop using new runner; no production migration needed. QA first pass
read-only against focused successor, attack leakage, hidden versions, provisional
labels, related samples, scope and cost claims. No self-awarded independent PASS.

Stop-check: general test runner, story-specific manifest only; no production impact;
bounded assessment spend under existing continue approval; no new product decision;
no changed creative output needing Cowork/Guy acceptance. Rejected: tuned-per-page
prompts, synthetic errors presented as real, automatic rendering based on this sample,
and routine Guy review as the eventual product workflow.

## Post-result diagnostic stage (declared before its calls)

Expansion completed12 known results: original/corrected repeat; five of ten new
provisional normals rejected. This disproves broad false-alarm closure. Next narrow
probe:4 additional assessments in a separate root/1USD planned reservation, selecting
original defect plus contact/occlusion failures7/9/10. Full source PNG plus one
unmodified crop, same frozen anatomy rubric. New wrapper explains view roles only;
no expected verdict, targeted critique or answer in the prompt. Coordinates selected
by Codex for this diagnostic, NOT an autonomous localization implementation.
This is post-selection diagnosis, not a held-out accuracy evaluation. No rerender.
Results from the completed full-only stage remain immutable. All crops and hashes
retained. Optional details preserve full-only input/cache identities. Test ordering,
crop bounds/identity, full-byte preservation and replay before running. Production
QA remains unchanged regardless of these four results.

## Owner clarification: severe failures only

Guy explicitly requested tolerance for non-realistic illustration and blocking only
clearly wrong severe defects. The detail probe retained all4 defect verdicts; crops
did not close oversensitivity. Implement a NEW, optional severe-only policy and
strict result schema: classified severe findings can produce defect; minor notes
are separate/nonblocking; malformed outputs remain unknown; uncertain never repairs.
No baseline rewrite or silent old-verdict reinterpretation. No resemblance change.
Test8 fixed full-image targets: original, corrected, known-normal gallery3, plus
five post-selected false-alarm cases7/9/10/11/12. Up to8 additional QA calls,2USD
planning reservation in a separate root. Existing key, no image/audio render/push.
Prompt and labels preregistered before calls; this is targeted calibration after
owner policy change, not held-out accuracy proof. Main-child anatomy only; not
continuity or whole-book release. Active pipeline still unchanged pending evidence.
Acceptance: preserved severe original rejection, fewer provisional-control alarms,
explicit nonblocking notes, strict severity parsing and immutable per-policy replay.

## Final bounded probe: verify the finding, not blanket leniency

Severe-only policy missed the original defect, describing a grip absent from that
image. Do not activate it. Test4 existing full-only detector results (1/7/9/10) as
untrusted proposals with the same source PNG, up to4 further calls/1USD reservation.
Separate schema: exactly one severe/minor/unsupported/uncertain assessment per ID;
software derives disposition, uncertainty holds, minor/unsupported cannot repair.
Another call to the SAME model is not independent QA. No ground-truth label supplied.
All findings and source report digest pinned before calls; post-selected diagnostic,
not general benchmark. No generation or active gate. This turn totals at most28
assessment calls across four bound stages;7USD reservation planning, not invoice.
