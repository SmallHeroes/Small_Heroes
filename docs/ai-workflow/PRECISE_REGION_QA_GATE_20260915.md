# Precise-region anatomy diagnostic — 2026-09-15

Owner requested autonomous completion, stopping after repeated unproductive attempts.
Same task, sole writer in C:/GNart/Work/sh-r3b1b-semantic-m1, codex/r3b1b-semantic-recovery-m1,
base c175586a57f658c5059481f2a66dde5ac646d74e, clean ahead24/behind0.
Protected d53b768ccb2f and accepted-intent63ccb484 remain read-only.

Observed: broad crops over-rejected controls, whereas severe-only and finding
verification both missed the owner-rejected original. Wrong-limb correspondence
is a plausible explanation, not established internal model reasoning.
Expected: reject the obvious fragment while tolerating the corrected illustration.

Use the EXISTING frozen runner, transport, prompts and parsers unchanged.
Two preregistered arms: style rubric and severe-only, each on original/corrected
with full image plus identical-coordinate 280x320 crop (left50,top700).
This region includes the suspicious flesh/blue-cloth fragment and nearby head,
shoulder and wing context. Implementer-selected oracle localization, NOT automatic
localization, independent QA, or a held-out accuracy benchmark. Corrected label
remains provisional. No expected verdict or defect description goes to provider.

At most4 Qwen assessment calls total,0.25USD reservation each,1USD total planned
reservation (not an enforced provider invoice cap),max3000 output tokens each.
Existing Replicate credential only; official async dispatch, retained IDs/receipts,
no POST retries. Provider gets illustration pixels and fixed rubric, no photos.
No image/book/audio render, active QA/default/threshold change, source/reader edit,
publication, deployment or push. Original reports remain immutable/local-only.

Acceptance: both arms must be inspected for actual localized visual evidence,
not merely overall verdict agreement. If both fail original/corrected separation,
stop this Qwen prompt/crop calibration track: repeated failure does not authorize
further spending. If a candidate separates the pair, further automatic localization
and independent controls are needed before activation; oracle success alone is not
a deployable solution. Unknown results hold and are not repair authority.

Validation: runner preflight/hash binding, crop visual inspection, existing targeted
benchmark/comparison tests, tsc, source/reader/report hash preservation and replay.
No production changes; full repository check remains NON-GREEN, not relabeled.
Rollback: cease using these fixtures. Scope is experimental assets/documentation.
Stop-check: bounded diagnostic under explicit owner continuation; no unresolved
product decision, no new architecture, no story-specific production patch.
