# Dini text-only publication — execution gate

## 1. Proposed change
Publish the exact owner-accepted revision-02 manuscript as a separate immutable creative-replacement source using the existing reviewed lifecycle. No code changes.

## 2. Why now
The missing v4 predecessor support was fixed at0951cc8a and reviewed by a delegated technical agent for39c528a4..0951cc8a. The editorial agent passed exact225f2b01... manuscript. Guy now explicitly answered yes to accepting that new editorial review and publishing text only without rendering. This closes the exact-review-binding prerequisite, not whole-repository stability.

## 3. Scope
Story-specific content publication through the general lifecycle, not story-specific code. Same task is sole writer at C:/GNart/Work/sh-r3b1b-semantic-m1 on codex/r3b1b-semantic-recovery-m1. Start3af5fec90e322a786e30461039b412a7564bd923, clean/ahead11 against local upstream. Dependencies d53b768ccb2f and accepted-intent-wave-263ccb484 clean/read-only. No fresh remote state inferred.

## 4. Hardcoding risk
No new branch, validator or override. Request data binds the actual v4 predecessor and exact source/review/brief. Existing runtime story selection remains unchanged.

## 5. Files
Local outputs execution request, preview/result/verification logs; one seven-file accepted revision directory; CURRENT.md, ROADMAP.md and this gate. Original pending packet remains historical and unchanged. acceptedAt is the observed approval time, explicitly not an invented original message timestamp.

## 6. Expected behavior
Preview validates before writes. Publication creates seven immutable files with story_text_only authority and runtimeEligibility.eligible=false. Self-contained reload validates all bindings. A repeated publication is an identical no-op. Old accepted source, package and all13 reader images retain original bytes.

## 7. Validation
Preview, publish, shared accepted-successor reload, idempotent replay, predecessor/full-book hash preservation and current package selection. Rerun13 lifecycle tests and npx tsc --noEmit. No production code changes: do not rerun the full costly repository gate merely for accepted data/docs; last full result stays5988passed/7timeouts/73skipped, NON-GREEN with no inherited classification. No delegated verdict is called Claude Code PASS. New publication artifacts have not yet received independent review.

## 8. Cost
Zero API/image/audio calls. Zero spend; no credentials.

## 9. Rollback
Leave the new non-runtime revision unconsumed; old runtime source remains active. Do not delete or overwrite historical revisions. Any later retraction is a separate explicit operation.

## 10. Review and owner decision
Owner decision is explicit yes to the exact review/text-publication question. Editorial review SHA9598627f72507b93ddd7f7722f167077a02b9db65b59cabab4be5e81d9b5fc36; story SHA225f2b01b3fa2247b0dbffcd86b3bcfd4c89b2a90ae38af8b1842b1341826fd8. Future QA should falsify request-to-artifact bindings, exact seven-file inventory, self-contained replay, old-book preservation and authority boundaries. No new prose was proposed and no further creative review is required for unchanged wording.

## 11. Do not do / stop-check
No integrated.md migration, runtime locator/story-bank replacement, Blueprint/visual directions/visual contract/package edits, render, deploy, push, threshold changes or fabricated independent PASS. No other story/child/style is changed. Smallest validation is local lifecycle execution and hash checks; no money is spent and no image needs eyeballing. Next visual planning is separate from this approved text-only milestone.

## Execution result

- Preview exited0; real publication created:true; identical replay created:false, both exit0.
- Revision f77f4ca51fe3692d283f9fd1354392776e358da7ead86ab24cbb7e6e0bca9e98; seven files. Manifest raw SHA cc7c81e1676be63f548c056d5be572281e536c0de1da81026ad7ddee24b52dfa.
- Shared loadAcceptedCreativeReplacement rebuilt and validated the actual accepted revision from its own files and genuine v4 predecessor. No dependence on staging request is passed to this reload.
- Read-only before/after helper verified28 pre-existing source/book-review files byte-identical,13/13 original images matching their manifest, old package4d6e8dee still selected and render-qualified.
- Local outputs: publication-request.json SHA f77cf15953bf3a7d4a1ba01294ee8634e665d123df152bcc7b3c52fc495d61c0; publication-result.json SHA71f47319b44220d7500c7cb3158357bee587732209e2808cc79eb64b24f7bc75; publication-after.json SHA41300b726c0807f0c2ad33a3d47d6fa54f0cef0687c25d5872518b5d6702f397.
- verify-publication.ts is read-only local evidence helper; invokes actual shared selection and accepted-source loaders, not a duplicate production validator. It rejects fetch. CLI uses the existing lifecycle directly.
- npx vitest run lib/__tests__/story-source-creative-replacement-lifecycle.spec.ts:13/13, exit0. npx tsc --noEmit:exit0. No full-check rerun and no current full-gate PASS.
- git check-ignore on new story.md returned1 (not ignored), not an execution failure. Seven accepted files will be explicitly staged; output logs remain ignored/local-only and are not preserved by a Git push. No verified remote backup.
- No old authority/file replaced, no code changed and no money spent. New publication artifact verification is Codex's verification, not the earlier agent's code PASS or a new Claude verdict.
- Whitespace exception: raw git diff --cached --check exits2 for exactly one existing extra EOF blank line in the hash-bound creative-brief.json (line116). The published file intentionally preserves the reviewed input bytes; trimming would change its identity. A one-command diagnostic excluding blank-at-eof exits0; no repository setting was changed and raw check is NOT reported green. All7 staged Git blobs were independently byte-compared with published files and match exactly.
