# Dini owner-accepted rewrite — source preparation gate

## Intake and owner decision

Guy said "מעולה תמשיך לביצוע" after receiving revision02. Codex treats this
as acceptance of that exact wording and authorization to proceed to source
preparation, not independent Editor PASS or paid Blueprint/image authority.
Candidate raw SHA225f2b01b3fa2247b0dbffcd86b3bcfd4c89b2a90ae38af8b1842b1341826fd8.
No repeat approval of unchanged prose is requested. New editorial findings that
change the wording would need product judgment; no blanket acceptance is inferred.

## Observed vs expected / cause

Expected: create a separate immutable accepted text successor, then separately
derive and review visual directions, reconcile Blueprint/package and choose
rendering. Preserve old accepted revision64dcd0e7,package4d6e8dee,Blueprint1125b395.

Actual: candidate passes the real editorial-format validator in gender_flexible
mode, exactly12 pages, and both real materializer gender projections. The
validator's name validateEditorialPassDraft is NOT an editorial verdict.
The currently bound predecessor is manifest/v4. The existing creative replacement
CJS lifecycle loadPredecessor accepts only manifest/v2 or creative-replacement/v1;
therefore it cannot consume this v4 predecessor. This is source-inspection proof,
not a fabricated publish/dry-run request or claimed failed provider execution.

A full v4 validator already exists in the shared TypeScript authoring authority
loader. Read-only execution proves that the actual predecessor passes it. Its
integration into the CJS publisher is NOT implemented here. Merely adding v4 to
the old version allowlist would omit required full authority validation.
The current creative-replacement path also requires an exact external editorial
PASS and Guy approval of its bytes. No such review exists for this manuscript.

## Scope, risks, alternatives

This milestone is provider-free preparation/evidence only: local read-only
preflight, owner-content observation, review handoff and canonical status.
No production behavior or source authority changes. Do not fork from an older
v2 ancestor merely to satisfy the allowlist, copy old directions to new prose,
construct an empty PASS, or relabel owner approval as independent review.
Do not replace integrated.md or point the existing package at the new text.

Recommended technical successor is a general v4 predecessor adapter that reuses
the full shared validator and preserves existing v2/creative-v1 behavior. It
needs explicit failure tests for tampered predecessor inventories, identities,
acceptance/review bindings, paths and fork collisions, plus CLI/loader replay.
The CJS/TS dependency and downstream self-contained loader must be handled
together; an allowlist-only patch is rejected. This is a proposal, not implemented
or independently approved code. No story-specific production branch is proposed.

## Execution / proof

Same Codex task sole writer at C:/GNart/Work/sh-r3b1b-semantic-m1,
codex/r3b1b-semantic-recovery-m1; start98409d28,clean/ahead8 vs upstream90dfd387.
Protected dependencies768ccb2f/63ccb484 clean/read-only. No parallel writer.
Script: outputs/dini-story-rewrite-20260913/execution-01/preflight.ts.
First attempt failed MODULE_NOT_FOUND from an extra ../ in the new helper's
imports; corrected helper, preserved first logs. Final invocation exit0:

- exact candidate hash/canonical format validated with shared validator;
- boy6692bytes, SHA47c2e6f99255127baa0e599da74969cacf5d8f1ea45bbaafcc1e8dda4b84a374;
- girl6762bytes, SHA8a21cb775495d97f2ff63c8d5acfe9e2128a91b72bc8150d13d298205e430bc7;
- actual v4 predecessor passes shared authority loader;
- old source SHAa5942c36... and first draft SHAe73ac4c0... unchanged;
- existing package4d6e8dee still render-qualified;13/13 original image hashes intact;
- providerCalls0, publicationPerformed:false, independentEditorialPass:false.

Result: preflight-final.json, original failure logs retained separately. Script
prints only; shell redirection creates logs. Outputs are ignored/local-only with
no verified off-machine backup. This is not new-source publication eligibility.
No imageDirection, source revision, acceptance lifecycle request or release artifact
was manufactured. No credential reads, paid calls, rendering, deployment or push.

## Boundaries and handoff

Ready for independent editorial/artifact review of exact candidate, not source
publication. Separate owner observation is not a publication request and binds
no nonexistent editorial review. Technical source-lifecycle adaptation remains
unimplemented. Full repository check remains NON-GREEN (prior20 timeouts), not
rerun for this preparation. Reader predecessor QA and cake/cart validation remain
open. Page7 acceptance coexists with original safety_failed/null numeric evidence.
Independent code PASS ends at the supplied81738ff1..ca29d599 range, not this work.

Stop-check: preparation does not touch any live source, identity, bank, reader,
prompt or provider. No new creative decision required; no current image to eyeball.
Publishing is paused for missing independent review and compatible lifecycle;
these are not additional requests to reapprove the same story. Rollback: leave
the separate preparation unconsumed; no old artifact needs restoring.
