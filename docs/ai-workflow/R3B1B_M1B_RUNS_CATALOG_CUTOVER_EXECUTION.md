# M1b remaining running/catalog cutover

Date: 2026-09-08. Status: code review checkpoint; FINAL FULL CHECK NON-GREEN,
independent QA pending. Not completed M1b and no downstream authority.

## Authority, topology and observed cause

Guy approved continued work through a bounded render; the general semantic
recovery Decision Gate still governs M1 then M2 then exact-artifact acceptance
and downstream qualification. This slice is offline/$0. No full-book, provider,
Blueprint, image, publication, payment, deployment, source mutation or push.
Existing API-key reuse was owner-selected earlier; presence alone checked in
the main project's existing configuration, never copied, printed or changed.

Sole writer: current task, `C:/GNart/Work/sh-r3b1b-semantic-m1`,
`codex/r3b1b-semantic-recovery-m1`, base `d91fc72b6ea7ebe81202ca6e68adc7a80393b89b`.
Start clean/cached ahead 5 behind 0. Protected dependencies clean/read-only:
d53b at `768ccb2f`, accepted-intent wave 2 at `63ccb484`. No topology cleanup.
Claude's supplied group PASS on `fe07a65e..d91fc72b` was reconciled previously;
294 tests / 30 attacks / both typechecks, not Codex's overlapping 332-test set.
Full-check log verified by Claude, not rerun. That PASS does not cover this diff.

Observed: v3 lacks running; compiler/schema/catalog imports are shared with
request construction, repair admission and receipt verification. A global enum
bump also changes old prompt/schema signatures and invalidates exact replay.
Expected: new authoring/projection supports literal running; old v3 artifacts
retain their original semantics and read-only replay without rebinding receipts.

## Solution, scope, exceptions and rejected alternatives

- Catalog v4 appends `runs`; cast-only, object/laterality/spatial effect and
  spatial constraint forbidden, just as walks. No new group motion or spatial
  powers. Hurried/skipped motion is not automatically classified as running.
- Frozen v3 definitions and digest unchanged. Legacy schema projection removes
  exactly the one new enum member from its existing walks signature group.
  Shape, definition order and every other field are preserved. This bounded
  projector refuses an unexpected runs-only schema shape; it is not a generic
  future-catalog migration. Nine historical provider-schema digests and both
  prompt digests were measured at protected `768ccb2f`, not repinned from new code.
- Policy is per invocation, with cloned historical schemas. No mutable global
  version switch, no string substitution in source or archived artifacts.
- Current compiler defaults to v4. Offline replay selects v3 explicitly for
  request-v55/receipt-v58 route v1 or request-v56/receipt-v59 route v2 with v3
  catalog binding. Historical injected responses cannot introduce current-only
  predicates. The public request factory cannot select historical authority.
- Request/receipt/candidate envelope shapes are unchanged. Catalog identity is
  already an independently versioned, digest-bound field; those enclosing
  versions stay v56/v59/v9. Prompt/schema shape versions likewise remain while
  content digests change and the prompt names its v4 catalog. The frozen v9/v3
  candidate reader remains strict; a separate dispatch selects v4 authority.
  No old envelope is relabeled as current. Group-bearing v5 contracts still do
  not enter this paid v9 route: supporting-cast preview discrimination and the
  reader's v4-template requirement remain in force. M2 introduces its own
  separate effective correction artifact, never a forged provider candidate.
- Current receipt prompt checks bind the request's catalog version AND digest;
  candidate factory still requires current receipt coverage/catalog bindings.
  The old paid receipt must use historical read/replay, not the current factory.

Rejected: aliasing runs to walks, changing old evidence, widening the frozen
reader, relaxing current factory checks, process-global compatibility flags,
increasing budgets, adding story/page branches, or generating another paid draft.

## Consumer inventory

| Consumer | Disposition / proof |
| --- | --- |
| Catalog/types/prose/contract validators | Current shared enum gains runs; all static rule/schema groups are exhaustively tested. No validator fallback or source-motion inference added. |
| Template initial/full-draft and bounded repairs | Per-invocation schema vocabulary; initial/full-repair prompts select the catalog. Other prompt text unchanged. All nine old schema hashes retained. |
| Offline repair harness | Explicit optional catalog policy, current default; injected old-lane runs rejected, no provider implementation. |
| Authoring lifecycle | Private legacy request reconstruction, current-only public factory and receipt checks; current metadata binds v4 digest. |
| Replay runner | Exact tuple + legacy artifact validation before selecting old wire policy; route v1/v2 remains separately bound. |
| Candidate/reconciliation reader | Explicit v3/v4 dispatch; fixed old catalog tuple never accepts new predicates. Shape unchanged, source/template/coverage identities still checked. |
| Blueprint schema/program/provenance/affordance/wire | Current draft schema v9 carries runs; frozen v6/v7/v8 retain v3 vocabulary. Exact former-current v8 execution program frozen as replay-only; compatible old provenance retained without admitting fresh dispatch. |
| Runtime/materializer/adapters/review | Existing generic action/prose path; literal runs projection available without story-specific handling. This is not image-quality evidence. |
| Corpus/representability audits | Current catalog identity; a spatial station still cannot run (subject-kind rejection, not now-missing predicate). |
| Source/cast/group authority, coverage semantics, cover-only bridge v5 | Unchanged; M2 must atomically correct effective actions, prose, references and coverage. |

The initial request increases exactly 80 bytes (73 prompt + 7 schema); all 18
approved Story Sources and QA 8/12/16-page sources remain inside the unchanged
80K admission ceiling and safety margins. No price, retry, timeout or worker
policy changes. Other repair schemas grow only where their vocabulary contains
the existing walks group; historical projections retain exact prior bytes.

## Validation history and limitations

165 focused tests passed across five specs; includes synthetic running through
compiler -> receipt -> candidate -> reconciliation reader and old-lane rejection.
Synthetic action testing is not source-semantic approval. Nine schema fingerprints,
two prompt fingerprints, interleaved policy isolation, unknown policy/shape
rejection and frozen envelope cross-binding attacks are covered.

Held P1 replay: providerCalls 0, exactCapturedCallSequence true, receipt outcome
and candidate digest congruent; template `8632bd95869b93bf115500f3a24890daaa967a39805079e2f0ae86afc2543b18`,
candidate `efbdd2e13c03af194af425c5e050e0bfedad29d4e0c8cec1aa8b54530192ad88`.
Historical reader returns identical evidence; current factory rejects its receipt.
The diagnostic no longer reconstructs that receipt via the current factory, as
the approved Gate explicitly requires after cutover. Original evidence unchanged.

Early focused attempts exposed old current-only expectations (v3 catalog, station
missing-predicate reason, input byte totals) and one use of unsupported Array.at
in a new test. Corrected tests preserve frozen hash pins and explicitly account
for the new current vocabulary. No failure is classified inherited or hidden.
The first complete check failed: ordinary 8 failed / 4927 passed / 73 skipped;
resource-intensive 1 failed / 670 passed. This exposed a real Blueprint legacy
schema alias to current predicate vocabulary, plus changed current-only
schema/program/byte expectations and the one-new-spec inventory count. No
historical artifact or stored fingerprint was repinned to current values.

Corrective implementation within this uncommitted milestone: Blueprint draft
schema v9 is current; v6/v7/v8 schemas retain exact old fingerprints, and the
former-current v8 program `0944bdb56a83368e6c22feb886f0cfeed3b9a195ad01918e5ffd7d61de275f4b`
is an absolute frozen snapshot measured at protected `768ccb2f`. It is replay
supported, never current/fresh-dispatch authority. The new program differs only
in schema version/digest and resulting program digest. Historical schema-v6
request-v4/receipt-v6 fixture bytes and hashes are untouched and replay passes.
The existing synthetic test which merely relabeled a *current* Visual Contract
capture v55/v58 now correctly expects rejection: current captures carry v4
semantics and cannot be made v3 by changing envelope labels. This is not a
positive full-receipt v55 replay proof; the exact held receipt proof is v56/v59
with catalog v3. Old v1 routing and frozen wire schemas retain their regression
coverage. Independent QA should challenge this older-lane limitation explicitly.

Second focused run: **396 passed across 13 specs** (`focused-final.log`). The
initial 165 are a subset, not an additive total. A follow-up input accounting
expectation was corrected to the measured +7 Blueprint schema bytes. The full
second check is running; no all-green claim yet. Inventory is now 387/365/22
(one new ordinary spec), workers still 4/2; no runner classification policy change.
Final counts and handoff were pending at that observation; the checkpoint below records the outcome.

The second complete run (`full-check-final.log`, despite that filename) was
**not green**: ordinary 4936 passed / 73 skipped; resource 670 passed / 1 failed.
Only `canonical-materialization-input.spec.ts` / arbitrary CLI flag order timed
out at the unchanged 5000ms deadline. The unchanged full spec then passed alone:
15/15, the affected test 3276ms (`materialization-timeout-isolated.log`). This is
timing variability evidence, not proof the timeout is fixed or inherited. A
third complete run is in progress. No deadline, worker or runner change, no
excluded test; the failed log is preserved. Original P1 inventory was rechecked:
14 files / 412516 bytes, raw SHA-256 `cd621f7712912a92d14fa87432c609c5e92a006363e7eed7be5231a856e8fdd0`.

## Final checkpoint (supersedes in-progress observations above)

Third complete attempt (`full-check-3.log`) is **NON-GREEN**, exit 1. Both
typechecks passed; ordinary 348 passed / 17 skipped files, 4936 passed / 73
skipped tests (128192ms); resource 21 passed / 1 failed files, 670 passed / 1
failed tests (280238ms). The only failure is the pre-live readiness drift test:
prepare rejected at supervisor verification with `git_command_failed`, no
credential/provider access. The preceding materialization timeout did not recur.

The unchanged pre-live test then passed alone (1 passed / 13 deselected,
16549ms) in `pre-live-git-isolated.log`. Together with the 15/15 materialization
rerun, this demonstrates differing outcomes, **not** an established native
failure cause, permanent fix or entirely inherited baseline. Default supervisor
Git commands are bounded at 10000ms and mask native errors to the closed reason;
the third log does not reveal which native cause triggered it. No fourth blind
full rerun, no worker/deadline/skip-policy edit, no relabeling of any failed run.

The checkpoint has focused code coverage and a passing ordinary phase, but is
intentionally not reported as a fully green milestone. Independent review is
requested on an immutable local commit with this repository-gate limitation
explicit. Original candidate remains HELD and M2/downstream qualification is
not claimed. Cached Claude Code status reported an existing subscription login;
at that checkpoint Codex planned read-only QA after committing, without story-provider access,
secret reads, code edits, push or render permission for the reviewer. The QA
outcome is not inferred from authentication or from this planned dispatch.

At checkpoint preparation both typechecks were rerun and passed (exit 0), and
the held replay was rerun with all congruence fields true, providerCalls 0,
historicalReaderIdentical true and currentFactoryRejectedHistoricalReceipt true.
`git diff --check` passed. Final complete attempt log SHA-256:
`ab133ccd573e3b208a95cd9d3b116a62d2e24bc4b35b4d0d7b69a11fb0feb3cc`.
This binds a **failed** complete run, not a green one. Final logs live under
`outputs/qa-m1b-runs-20260908/`; exact immutable range and commands follow in its
ignored `HANDOFF.md`, after the focused local commit. No push is part of this task.

## Independent QA dispatch result — authentication blocked

Code checkpoint `ff51b3e763f81737c7e45536ff5425253b0b7971`: clean after commit,
cached ahead 6 / behind 0, 29 files +679/-108, no push. Claude Code was invoked
with only Read/Glob/Grep/Bash tools, a read-only command allowlist, no session
persistence, and the exact immutable review brief. Application credentials and
story providers were excluded. Server authentication failed with HTTP 401 before
any review/model tokens; final result is_error true, input/output tokens 0,
total_cost_usd 0. It requested /login. Local cached loggedIn status was not a
successful server-authentication proof. No QA verdict or independent PASS exists
for this range, and no reviewer process remains active. This documentation
follow-up records the blocker without altering the implementation, full-gate
result, original P1 artifacts, or any downstream authority. Guy must reauthenticate
Claude Code; Codex has not read, copied or replaced its credentials.

Rollback: focused local commit can be reverted; no persisted production migration
or authority promotion. Original candidate remains HELD P0=0/P1=3/P2=3.
Independent QA then M2 atomic recovery remain mandatory; broad render permission
does not waive them. M2 must handle schema v5 -> v4 when the last group is removed.
