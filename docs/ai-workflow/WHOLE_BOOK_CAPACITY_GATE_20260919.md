# Whole-book capacity correction gate

Owner authorizes implementation then Claude QA. Same task, sole writer on
codex/r3b1b-semantic-recovery-m1, C:/GNart/Work/sh-r3b1b-semantic-m1. Base5e990881,
clean/ahead10. Dependencies d53b768ccb2f and accepted-intent63ccb484 clean/read-only.
Claude HOLD0/1/1 on093d37c0..5e990881. No push or independent PASS claimed.

Observed:14000 planner output ceiling unchanged for a larger joint payload. Real
Dini plan usage and sparse Panda ledger imply risk, not measured16-page failure.
Incomplete responses are paid, retained and terminal for that root. Repair appends
observation+correction to a long prompt and can hit24000 after earlier paid images.
Transport separately checks32000 including prefix and multipart CRLF expansion.

General local-preview fix: page-scaled output ceiling, input-byte+protocol admission,
matching30USD/M upper-accounting reservation and versioned identity. Same model,
effort,10USD run fence and no retry. Minimum upfront budget is the larger of legacy
nominal image floor or planner+first-image allowance, NOT a worst-case full-book/QA
completion promise; every subsequent paid step checks remaining funds.

Compact repair projects all effective state and every full correction. Historical
invariants and verbose observations remain in original plan/QA, not duplicated in
the edit prompt. No correction truncation or changes to camera/acting/state. Test
worst-schema repairs for ALL pages before FIRST image, including real transport
prefix/CRLF and bounded predecessor metadata. Oversized plans hold before images.

Scope: local-preview only; owner-draft capacity and production migration not claimed.
No story/anchor/calibration/threshold changes. Test actual runner8/12/16, provider
request/reservation binding, terminal replay, last-page oversize before board,
all8 maximum corrections, saved Panda size witness, preservation/typecheck/fullcheck.
Rollback focused commit; retain paid evidence; new policy requires fresh output root.

Stop-check: approved engineering repair, no product choice needed. Cost0, no provider
or key reads/renders. Claude attacks capacity/projection/replay boundaries; Guy later
judges pixels. Reject silent clipping, weakened QA and automatic paid repurchase.

Official limits checked2026-09-19 (not a utilization guarantee):
https://developers.openai.com/api/docs/models/gpt-5.4 (128000 output tokens)
https://developers.openai.com/api/reference/resources/images/methods/edit (32000 chars)
