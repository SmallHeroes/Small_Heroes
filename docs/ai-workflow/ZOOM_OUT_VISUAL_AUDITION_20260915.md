# Zoom-out direction audition (not engine validation)

Guy approved two examples after being told existing frames have not been rerendered.
Base85dd7f1855c9a92bf892701f0ceb23985a3ed061, semantic-m1, clean ahead25 at start.
Same coordinator is sole repository writer; image workers may generate only isolated
native-tool outputs, not edit code, credentials, original assets, or production state.

Observed: current narrated reader uses old PNGs; original plan had3wide/6medium/3close,
no structured numeric framing. Current local-preview code has35% wide child-height,
50% medium ceiling and at least50% environment, but paid runner calibration is HELD.
Reader also uses cover/masking; this audition concerns painted composition, not CSS.

Produce exactly2 built-in ImageGen reference-based zoom-out illustrations from pages
2 and10, isolated visual directions. Full figures roughly30-35% frame height,
environment over half, scene action/identity/wardrobe/design preserved as far as the
model permits. Do not assert pixel-exact preservation or fix all pre-existing defects.
No extra variants/retries without a meaningful reason. No direct API/credential use;
built-in tool cost/model/quality settings are not independently metered here. This
is NOT the gpt-image-2 LOW production runner or evidence its numeric policy works.

No judge/default/threshold/calibration changes, no gate bypass, no book/reader
replacement, no source/audio edits, no publication/deployment/push or release PASS.
Existing cake/bridge continuity defects do not become accepted through this audition.
Source hashes and final paths/prompts preserved in a local report. Inspect outputs
visually for framing and note deviations; no automated or independent QA claimed.

Stop-check: owner direction explicit; bounded2 illustrative outputs, not fullbook;
general goal, story-specific audition references only; no production risk/migration.
Rollback: discard audition selection, original reader unchanged. If useful, future
integration must validate the real renderer separately without changing judge status.

## Completed results and read-only QA handoff

Exactly2 built-in calls, no retries/direct API/credential reads. Full prompts and
before/after comparison under outputs/zoom-out-direction-20260915/{REPORT.md,index.html}.
Street PNG SHA256 ee7c7dc4c3854e1d515af1a708ef4fe6650cead0016b0ccea5158715379877d7
Slope PNG SHA256 c033f7fb76c0d6485026e35ef59352c53daaf543893c2a763d1b9b3fac3e16cf
Both1024x1536 images visually inspected by coordinator. Much more environment;
estimated boy height24-25%, below requested30-35%, so not numeric compliance proof.
No blank-padding trick, but broader surroundings are invented/recomposed. Slope
prompt requested3tiers despite source's known inconsistency, so not a controlled
framing-only experiment; stone bridge remains, no book continuity acceptance.

Image workers only generated isolated outputs; no repository write authority.
Creative board direct UI route unavailable here; used direct image/file delivery.
Local artifacts not preserved by Git/push; no off-machine backup verified.
All30 scoped reader/media/manifest and engine files unchanged, aggregate
e64a86a29a73dae8c1ed3f4f48f187f6d50162778b00c31e6a6261f03cb7285e.
tsc0; prose/artifact-only successor, no code tests/fullcheck rerun. No independent
technical PASS. Review base85dd7f1855c9a92bf892701f0ceb23985a3ed061 to focused commit
containing this handoff. Attack engine-vs-direction distinction, original preservation,
numeric compliance claims, source continuity drift and generated-image identities.

Copy-ready inspection/optional push (push not performed; entire ahead set travels):

```powershell
Set-Location 'C:\GNart\Work\sh-r3b1b-semantic-m1'
git status --short --branch
git log -1 --oneline
git diff --stat 85dd7f1855c9a92bf892701f0ceb23985a3ed061 HEAD
git log --oneline 'origin/codex/r3b1b-semantic-recovery-m1..HEAD'
# Only on explicit owner push direction, after reviewing the ahead set:
git push origin HEAD:codex/r3b1b-semantic-recovery-m1
```
