# Child performance and material composition — approved offline engine milestone

1. Change: general situational child expression/micro-acting and a versioned
   hero-specific composition policy for new Blueprint authoring. No story rewrite.
2. Observed: all13 selected Dini prompts force attentive_neutral; Hebrew child
   laughter is missed. Composition v1 measures largest cast, allowing companion
   scale to mask monotonous hero placement. Camera labels alone do not fix this.
3. Scope: renderer expression resolver/assembly, Blueprint composition policy,
   versioned authoring guidance and historical-program compatibility, tests/docs.
4. Generality: no Bar/Dini/page-number table. Child identity stays fixed; expression
   and micro-acting vary within approved actions/eyelines/geometry, not new events.
5. Likely files: style01-visual-polish, style01-prompt-assembly; preRenderBlueprint
   policy/types/validator/authoring/contract; execution-program registry; existing
   expression/composition/authoring regression specs; CURRENT/ROADMAP.
6. Expected: child-specific evidence wins, bilingual/niqqud text understood for
   bounded cues, action context supplies engagement when prose is silent. Unknown
   context is not a forced neutral face or universal grin. New plans reject near-
   identical hero staging despite relabeled shots; old approved plans are unchanged.
7. Validation: before/after regressions; actual assembly and durable validator;
   historical v1/program fixtures; stored13-page prompt-only audit; full check/tsc.
8. Cost: ZERO provider/image/audio calls. Existing-key presence only checked under
   prior explicit reuse decision; no credential passed to a provider.
9. Rollback: focused local commits; legacy policy/prompt/program bytes preserved.
   Do not mutate approved package/source/Blueprint/images or old evidence.
10. Owner: Guy explicitly requested engine improvement now, story improvement later,
    no renders now. Codex sole writer in current task, Claude paused; no independent
    self-PASS. Later Claude falsifies actor attribution, constraints and replay.
11. Exclusions: spend, story edits, new package activation, database, thresholds,
    customer release, reader redesign, deployment and push.

Stop-check: general system fix touching prompts and planning, explicitly approved.
Risks: wrong actor emotion, inappropriate cheerfulness, new physical action invented
by micro-acting, and retroactive geometry rejection. Mitigation: child-scoped cues,
bounded semantic action evidence, no forced smile, frozen-frame precedence, v1
compatibility with new version opt-in for current authoring. No unresolved product
decision: restrained plausible reactions are allowed even without explicit text.
Rejected: story-specific expression lists, random emotion cycles, camera changes at
render time, forcing every page to smile, and rerendering before engine proof.

Topology: C:/GNart/Work/sh-r3b1b-semantic-m1, branch
codex/r3b1b-semantic-recovery-m1, base a5b93ddc, clean/ahead3 behind0 locally.
Protected d53b768ccb2f and accepted-intent-wave-2 63ccb484 clean/read-only.
Same task continues to avoid overlapping writers. One bounded engineering commit
unless valid findings require a separate correction. Guy judges new images later.

## Engineering evidence

- Before implementation, the 8 added regressions failed (17 existing passed).
  The failures reproduce missed Hebrew laughter/smiles, forced-neutral fallback
  and camera-label-only variation. Preserved child-performance-before logs.
- Final focused run: 244 tests passed in 9 existing specs. Actual renderer
  assembly proves positive child-owned actions, excludes must_not/other-actor
  actions, preserves the whole projection, and keeps dense page7 requests below
  the unchanged 30,000-character headroom check in LF and CRLF forms.
- The first full check returned exit1: 14 ordinary failures, resource671 passed.
  Found and fixed: new prompt excess (30,105 vs30,000), old live-version/byte
  expectations, historical fixture using a mutable current prompt builder, and
  a current fixture that varied camera labels without materially varying staging.
  The last fixture also needed to stay inside its required action spaces.
  No test ceilings, provider budgets, retry counts or quality thresholds increased.
- Historical prompt v9 bytes/digest and repair v11 digest remain exact. The old
  program 6d7a87f05bc9209d8225ca55168494506c0da4fb2df956701a5039748e771d78
  is frozen, replay-supported and not current. New initial/repair bytes are
  3685/4152; the production-scale harness accounts all extra bytes under unchanged
  spend/token admission, preserving its non-convergence result.
- Shared assembly on the actual bound Dini package: page1 joyful, page6 playful,
  other11 situational; 13/13 original image hashes match; projection byte-unchanged;
  existing composition-v1 package remains qualified. Hypothetical v2 detects
  near-identical hero placement on pages6-8. No candidate or images regenerated.
- Second full check exit1: ordinary5275 passed/73 skipped, resource669 passed/2
  5000ms timeouts in unchanged canonical-materialization-input.spec.ts. Previous
  resource run passed671. No inherited classification or full green claim.
  The second run's ordinary phase preceded a last one-line actor-name exclusion
  (Happy must not imply happiness); final244 focused tests and tsc passed after it.

Local evidence is ignored under outputs/, stored on this machine only with no
verified off-machine backup; a Git push does not preserve it. The audit script is
outputs/child-performance-offline-audit.ts, with output
outputs/child-performance-final-offline-audit.json (SHA256
1c550fd206310cc3964da3c5a53bf3f772b0fd3378639036549edf87de20d76a).
Focused stdout SHA256:
16431352699d42f9862fbc4ab8dda8ea184b932dbeb52fb604bbb35435a108a9.
This hash is the earlier243-test checkpoint. Final244-test stdout SHA256:
e66614183142a5f33402ef677cf289e9e0a092a39b1aafa5c89484c99e18acd6.
Second full-check stdout/stderr SHA256:
b3609d136b88c263868d81c58166c5de77e54b2be9458feb333c5f81063bd99f /
e19d5c794dbf52a77f2486a88e29bf35357deeefa73d1cefbc6496297592721a.

Isolated unchanged-spec recheck:14 passed/1 timeout (denied-boundary test,5000ms).
The CLI-ordering timeout did not repeat in isolation. No timeout/config/code edit
was made to that spec. Full repository stability remains open; this does not
block storing the focused tested engine milestone locally, but is not release QA.

Limitations: this milestone does not prove visual quality or model convergence.
Expression extraction is bounded heuristics, not full actor/coreference parsing;
unrecognized/ambiguous prose is left situational. Group actions are not promoted
to child-owned physical-action hints. Shorter than8 child-present pages retain
the existing calibration exemption. No story/Blueprint/package migration or
independent QA was performed; approved immutable assets are not retrofitted.
Resource-phase reliability P1 is not independently closed. Guy's page7 visual
acceptance coexists with original safety_failed and missing numeric evidence.
