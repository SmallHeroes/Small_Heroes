# R3-B1b P1-A1 Post-Cardinality Visual Contract Authoring — Decision Gate

Date: 2026-09-04

Product owner: Guy

Technical owner: Codex

Planning base: `be65b3ced874397a08dd2b2aa6283a5b85dfb857`

Attempt branch: `codex/r3b1b-p1-a1-post-cardinality-authoring`

Status: **ZERO-COST GATE PREPARED; BRANCH PUSH, FRESH READINESS, PROVIDER/SPEND
GO AND LIVE EXECUTION PENDING**

## Approval record and boundary

Guy set the product sequence: finish bringing the stories to the complete
render-qualified state first, move to the remaining engineering work afterward,
and leave payment work until the end. He authorized Codex to start the next
technical preparation.

That direction authorizes this local branch and Decision Gate. It is not an
exact authorization to push, read a credential, call a provider or spend money.
The previous P1-A1 authorization was consumed by its failed-closed execution;
unused calls or dollars from that run are not retry authority.

This Gate covers one new execution identity for the Visual Contract of P1 only.
It stops before Blueprint, images, package work and every other story.

## Current topology snapshot

- Worktree:
  `C:\Users\guyna\.codex\worktrees\d53b\Small_Heroes`
- Branch: `codex/r3b1b-p1-a1-post-cardinality-authoring`
- Current planning HEAD:
  `be65b3ced874397a08dd2b2aa6283a5b85dfb857`
- Upstream: absent; ahead/behind is therefore not applicable.
- Pre-commit dirty scope: modified `CURRENT.md` and `ROADMAP.md`, plus this new
  ignored documentation file. No code, story, package, locator or output file
  is dirty.
- Write authority: this Codex task is the sole writer for this branch/worktree.
  The audit agents are read-only. The accepted-intent base worktree remains a
  read-only dependency source and is not part of this milestone's write scope.
- Before Fresh Readiness, the documentation milestone must be committed, the
  branch must be explicitly pushed to its same-name origin ref, and Git must
  prove clean `0/0` parity again.

## 1. Proposed change

Run the current, independently QA-passed Visual Contract authoring lifecycle
once for the exact accepted `dragon_dini_adventure` source, under a newly
materialized current-policy request and a new explicit provider/spend authority.

The attempt is not a code change and is not a replay of the previous live
request. It must bind:

- accepted revision
  `64dcd0e741f17fc08cde95ad8a5a00b303955aa28ccd065d44f01e49e9d155fc`;
- canonical source
  `story-pipeline/04_approved_story_sources/accepted/dragon_dini_adventure/revisions/64dcd0e741f17fc08cde95ad8a5a00b303955aa28ccd065d44f01e49e9d155fc/integrated.md`;
- canonical source SHA-256
  `a5942c3646d08bc037395851311e9627bdfc1dd29f7874a45b000b87af416630`;
- 12 pages, `grounded` world mode and neutral source-gender authority;
- production Style01 authority and Dini's six passed reference views; and
- exact technical base
  `be65b3ced874397a08dd2b2aa6283a5b85dfb857`.

Fresh artifacts must use authoring policy/routing `v22/v2`, authoring
request/receipt/readiness `v56/v59/v56`, candidate `v9`, B0 materialization
input/manifest `v44/v54`, execution materialization input/result `v41/v46`,
Supervisor request/readiness/result `v51/v51/v44`, and canonical pre-live
readiness `v51`. The historical request v55, receipt v58, readiness v55 and
routing v1 are immutable offline replay authority only.

## 2. Why now?

The Wizard exposes 18 nominal story slots, but the current environment is only
17/18 product-sellable and 1/18 render-qualified. P1 is the immediate catalog
blocker: its accepted source exists, but no exact-source Visual Contract,
Blueprint, Board/prop inventory, visual package or current locator exists.

The first P1-A1 live attempt correctly failed after a page-10 compact repair
created duplicate Action Semantic Coverage binding. The general graph-aware
recovery correction is now implemented and independently passed by Claude Code,
including the handoff re-gate. A new current-policy attempt is therefore the
smallest action that can advance P1. Blueprint or package work cannot begin
without a valid and product-accepted Visual Contract.

## 3. Scope

This is story-specific artifact authoring through a general, versioned production
system. The selected source and references are data inputs; no Dini, page-10,
child, companion or story literal may be added to production routing code.

The milestone has two strictly separated boundaries:

1. **Zero-cost preparation:** push/topology verification, canonical Fresh
   Readiness prepare/verify, focused local validation and exact evidence capture.
2. **One live Visual Contract attempt:** only after Guy's separate exact GO.

Successful preparation grants `ready_for_spend_gate`, not live authority.

## 4. Risk of hardcoding and invariants

The attempt must preserve these system invariants:

- current routing policy v2 is the only fresh production route;
- a complete pure cardinality frontier with no legal compact solution may use
  the existing bounded `full_draft` lane;
- `cardinality_escalation` provenance may not unlock the terminal cleanup call;
- ordinary existing `full_draft` and `book_surface_patch` cleanup behavior is
  unchanged;
- schema, source, action, coverage, cardinality, identity, continuity and
  reference validators remain final authority;
- no deterministic deletion, merge or creative relabeling of coverage records;
- no fallback, transport retry, ninth call or threshold change; and
- the per-page resemblance threshold remains `0.70`.

A provider draft that fails validation produces no candidate and no downstream
authority. Codex must not hand-edit it into acceptance.

## 5. Files and artifacts likely affected

Zero-cost preparation may write only ignored local dependency/output state:

- `node_modules/`, rebuilt by the canonical offline bootstrap when needed; and
- `outputs/r3b1b-p1a1-v22r2-02/`, containing current immutable request,
  snapshot, materialization, execution and readiness evidence.

The live attempt, if separately approved, may add only content-addressed ignored
authoring receipt, captured structured-response, candidate/readiness and
Supervisor-result evidence under that same root. Tracked runtime, schema,
story-source, package, registry and locator files are not changed by P1-A1.

After the attempt, tracked changes are limited to truthful milestone evidence,
`CURRENT.md` and any necessary handoff correction. New files under `docs/` must
be force-added because that directory is ignored.

Before bootstrap, the current ordinary `node_modules` directory — presently
only the pre-existing `.vite` cache — must be inspected, hashed where files
exist, and moved intact to the validated absent external preservation path
`C:\Users\guyna\.codex\worktrees\d53b\Small_Heroes-node_modules-vite-cache-p1-a1-attempt`.
The canonical offline bootstrap then starts from an absent repository
`node_modules` path. The resulting dependency tree must remain in place through
Fresh Readiness verification and any separately approved live attempt so the
verified dependency identity is not swapped underneath execution.

## 6. Expected behavior after the milestone

The zero-cost preparation must end with a current-version request bound to the
exact source and clean same-name origin parity. It must report:

- `status: ready_for_spend_gate`;
- `credentialAccess: none`;
- `providerCalls: 0`;
- `pricingAuthority: not_checked`;
- `canonicalPreflight: not_run`; and
- `liveAuthority: none`.

After a separate live GO, exactly one Supervisor `live` invocation may run.
Success is one fully validated 12-page Visual Contract candidate plus truthful
receipt, token/cost evidence and provider-free replay. Success stops for Claude
Code and Guy review. Failure also stops, without an automatic second attempt.

Neither outcome starts Blueprint work.

## 7. Validation plan

Before provider reachability:

1. require a non-main dedicated branch with a same-name origin upstream, clean
   tracked/untracked state, exact HEAD equality and `0/0` divergence;
2. run the public canonical Git probe against the ignored output root;
3. run canonical Fresh Readiness `prepare`, then `verify` with identical frozen
   values;
4. prove the source digest and newly rebuilt source snapshot identity;
5. run the current focused authoring/lifecycle/replay/pre-live suites, both
   TypeScript projects and `git diff --check`;
6. run the real Wizard readiness audit and preserve its zero-effect counters;
7. immediately re-check the official Standard-tier price authority; and
8. stop for Guy's exact provider/spend GO.

After live execution:

1. validate complete 12-page schema/source/action/coverage/cardinality/identity
   authority;
2. prove the exact call sequence, attempts, tokens and conservative cost from
   the receipt;
3. replay captured structured responses with zero provider calls;
4. prove no Blueprint, Board/prop, package, locator, render, narration,
   publication or deployment authority exists; and
5. commit the bounded evidence and hand it to Claude Code for adversarial
   read-only review before Guy reviews the candidate.

No page, cover, five-page sample or full-book render is needed for this gate.

## 8. Cost impact and exact proposed live fence

Gate preparation and Fresh Readiness cost **USD 0**. They make no model call and
do not read the credential file; only its opaque configured path is bound.

The proposed live boundary is:

- OpenAI Responses / `gpt-5.6-sol`;
- service tier `default`, reasoning `medium`, tools disabled, `store:false`;
- at most 7 standard calls and 6 standard repairs;
- at most 1 terminal-reference cleanup call and 1 additional repair, only when
  current policy permits it;
- at most 8 total calls and 7 total repairs;
- zero transport retries and no fallback;
- 80,000 maximum input tokens per standard call;
- 12-page output schedule
  `40,000 / 32,000 / 36,000 / 24,000 / 24,000 / 24,000 / 24,000`;
- cleanup limit 12,000 input / 1,000 output; and
- projected maximum **USD 7.656**, hard ceiling **USD 10.00**.

On 2026-09-04, the official OpenAI model page was checked and matched repository
price authority `openai-standard-pricing/2026-08-25-v3`: USD 4.00/M uncached
input, USD 0.40/M cached input, USD 20.00/M output, cache writes at 1.25 times
uncached input (USD 5.00/M), with the repository's conservative 1.1 regional
uplift. The official promotional Sol rates are stated as available at least
through 2026-11-21:
`https://developers.openai.com/api/docs/models/gpt-5.6-sol`.

Conservative arithmetic is USD 7.568 for the seven standard calls plus USD
0.088 for the single cleanup reservation. The hard USD 10 fence is not raised
automatically. Any price mismatch or admission arithmetic above that fence
must reject before credential access or provider dispatch and return to Guy.

## 9. Rollback and containment

Before live execution, rollback is to leave the branch unpushed or revert only
the documentation commit. Any ignored output cleanup is a separate audited
operation; immutable evidence is not overwritten.

Dependency preservation is explicit and recoverable. If bootstrap fails, Codex
first records the bounded failure evidence, verifies the resolved repository
`node_modules` target, removes only the newly created dependency tree, and
moves the preserved ordinary `.vite` directory back from the exact external
path. If preparation succeeds, the verified installed tree remains unchanged
through `verify`, the owner decision and any authorized live/QA work. At final
closeout or abandonment, removing that installed tree and restoring the
preserved cache is a separately verified local cleanup; the donor worktree is
never moved or modified. The preservation path must be proven absent before the
initial move and absent again after restoration.

After live execution, provider evidence is preserved even if the candidate is
rejected. No package/current-locator/database/storage state exists to roll back.
P1 remains unavailable for rendering and the catalog remains contained until a
later approved package promotion proves the required readiness.

## 10. Review assignment and owner decisions

### Guy decisions required

1. Explicitly authorize pushing the dedicated branch so canonical preparation
   can satisfy its same-name origin/`0/0` topology gate.
2. After Fresh Readiness evidence is returned, explicitly authorize or reject
   the exact live boundary in section 8.
3. If a candidate exists, accept or reject its exact digest and visual/product
   content before any Blueprint gate is prepared.

### Claude Code falsification targets

Claude Code should try to prove that:

1. the new request can bind the historical v55/v58/routing-v1 authority;
2. source/revision/snapshot, HEAD or reference-view identities can be
   substituted;
3. a cardinality-escalated full draft can unlock cleanup;
4. more than seven standard calls, one cleanup, zero transport retries or the
   USD 10 fence can be exceeded;
5. an invalid result can receive candidate/output authority;
6. receipt usage, cost or replay evidence can disagree with the actual call
   sequence;
7. any downstream package/render/publication state changes; or
8. the flow contains a Dini/page-10 production special case.

Claude Cowork is not required for this technical retry gate. Guy must eyeball
the exact Visual Contract candidate if one is produced. P6 story review remains
a separate product/creative track.

## 11. Stop-check disposition

1. **General or story-specific?** General lifecycle, exact P1 data input; no
   story-specific code.
2. **Could it break another story/style?** No tracked behavior changes here;
   current shared routing already passed independent QA. Cross-version and
   substitution tests remain mandatory.
3. **Production behavior?** The live phase calls the production authoring
   boundary; Fresh Readiness does not.
4. **Spend?** Preparation USD 0; live projected USD 7.656, hard USD 10.
5. **Smallest validation?** Current-version pre-live plus one Visual Contract
   attempt; no image or render.
6. **Guy decision?** Push authority, then exact live provider/spend GO.
7. **Claude target?** Authority substitution, budget escape, invalid candidate,
   replay disagreement and downstream effects.
8. **Cowork?** Not for this technical gate; yes separately for P6.
9. **Guy eyeball?** The full candidate/review packet before Blueprint.

All questions are answered. Execution is nevertheless stopped at the missing
push and live authorizations.

## 12. Explicit do-not-do list

Do not:

- treat Guy's priority direction as provider, credential, spend or push GO;
- reuse the previous live request, execution identity or unused budget;
- run Supervisor `live` before fresh preparation and exact approval;
- run another live attempt automatically after any terminal result;
- use fallback, transport retry, extra call or validation waiver;
- hand-edit a provider draft into a candidate;
- start Blueprint, Board/prop generation, page/cover/full-book render,
  narration, package assembly/promotion, locator change, source publication,
  database/storage/order/payment work or deployment; or
- change the `0.70` resemblance threshold.

## 13. Next sequence after this gate

If P1-A1 succeeds and both Claude Code and Guy accept the Visual Contract:

1. prepare a separate P1-A2 Blueprint Gate (projected USD 4.928, hard USD 5);
2. stop for Blueprint QA and Guy acceptance;
3. perform a zero-cost authoritative Board/prop census;
4. obtain a separate exact LOW image count and price GO;
5. assemble, qualify, review and approve package v5;
6. separately promote its current locator and prove 18/18; and
7. only then begin P2, followed serially by P3, P4, P5, P7 and P8.

Payment integration remains last in the owner-selected order. Render
qualification itself does not require a story-page or full-book render.
