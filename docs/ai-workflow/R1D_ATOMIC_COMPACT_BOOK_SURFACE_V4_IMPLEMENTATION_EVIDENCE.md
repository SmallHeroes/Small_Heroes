# R1D Atomic Compact Book Surface v4 — Implementation Evidence

**Date:** 2026-08-17

**Status:** implementation and repository validation complete; independent Claude Code technical PASS; reviewed implementation commit pushed; no Fresh Readiness, provider, candidate, Wizard or render authority

## Topology

- Worktree: `C:\GNart\Work\sh-wt-r1d-output-budget`
- Branch: `codex/r1d-atomic-compact-book-surface-v4`
- Exact Decision Gate/base commit: `f669a192835ea1a36ec15dbd2310ce861901d39a`
- Reviewed implementation commit: `c73c22d6a2425bade902103566efba6a1468debc`
- Pushed, independently passed parent: `48ef78ff77f325b111219654b463608792fd2b39`
- Production: untouched
- Implementation/test provider cost: `$0`

## Consumed evidence

The immutable predecessor attempt is:

`C:\GNart\Work\sh-wt-r1d-output-budget\outputs\r1d-post-admission-fallback-readiness-48ef78ff-20260817T135224951Z`

Its authoring receipt v32 is
`1a69883d3b55b053f85a8d3361a241dd8cb1949067a7ffbbf28f28007c25d2db`.
It records three completed logical provider calls, two repairs, zero transport
retries, no fallback and `$0.954122 / $1.050246` nominal/conservative cost.

Attempt 1 produced nine exact page-spatial reference failures. The compact
spatial patch resolved all nine and exposed one presentation gap plus cover,
all twelve page final-structure failures and recurring-prop lifecycle. The
final presentation patch resolved its exact target, but fourteen structural
issues remained after the ordinary three-call/two-repair budget was consumed.
Supervisor v19 recorded a child nonzero exit and null output authority.
Candidate, reconciliation, Blueprint, Wizard and render authority are absent.

## Root cause

Book Surface v3 sends the full cover, full recurring-prop collection and full
affected page contracts, including unrelated action-semantic coverage. Its
mixed authority can exceed the canonical route-admission envelope. The prior
safe split works when two repairs remain, but after a spatial predecessor it
spends the final repair on presentation and leaves no call for the structural
surface. A generic fourth standard call would violate the frozen `$5` fence.

The final-slot admission refusal also lacked a truthful lifecycle terminal: it
would stop before dispatch but fall through to generic local-processing failure
without its exact accounting and carried typed diagnostic trail.

## Implementation

### Atomic Book Surface v4

The existing `book_surface_patch` mode now uses schema, system prompt and user
prompt v4. Its output contains:

- exact presentation patches;
- an authorized cover or `null`;
- authorized recurring props or `null`; and
- exact ordered structural page patches.

Compiler-owned input authority includes a local source-draft digest and compact
structural page projections that omit `actionSemanticCoverage`. The applier
prevalidates all components, clones once, and proves whole-draft non-target
equality. It preserves exact presentation pointer/value authority, requires the
selected `mustShow` value to remain exact through structural repair, permits
only `firstRevealPage` lifecycle mutation, preserves prop identity/content,
rejects stale or cross-location authority, and leaves the input unchanged on
every rejection. Full compilation and every validator rerun after application.

The proven route is:

`initial -> page_spatial_reference_patch -> book_surface_patch -> candidate`

within exactly three calls/two repairs and `[40,000, 32,000, 36,000]`.

### Bounded route admission

When mixed v4 is inadmissible and a later structural repair slot still exists,
the existing independently admitted presentation-first split is preserved.
When no such slot remains, the compiler throws a sanitized typed route-admission
error before provider dispatch. It carries only the repair mode, repair ordinal,
canonical byte accounting, the exact `59,904` maximum and a diagnostic count;
it carries no prompt, schema body, draft, response, provider message, path,
credential or stack.

The Visual Contract terminal remains one shape with a third nullable detail
slot. `repair_route_input_not_admissible` maps to provider admission,
input-limit violation and ineligible repair. Receipt v33 records only the two
actual calls and one actual repair; there is no synthetic third attempt or
usage. The route-admission detail is bound to final standard attempt 3,
`book_surface_patch`, the exact two-call/one-repair receipt trail and its
carried diagnostic count. Readiness v31 copies the exact terminal and
revalidates the current receipt, canonical digest, source/request/receipt
bindings and draft trail.

### Minimal version cutover

Current authority is:

- Visual Contract request/receipt/readiness: v30/v33/v31;
- B0 input/manifest/verification: v19/v28/v28;
- execution materialization input/result: v18/v22;
- Supervisor request/readiness/result: v27/v27/v20;
- Fresh Readiness evidence: v27.

Unchanged authority is policy v12, standard attempt budget v2, candidate v9,
OpenAI authoring evidence v6, child-output authority v1 and QA Wizard bridge v2.
Request v29, receipt v32 and readiness v30 are immutable legacy versions. The
legacy Visual Contract terminal without the new nullable slot remains readable
but cannot pass the current validator or writer. No historical bytes changed.

## Regression contract

The focused suite proves:

- exact mixed v4 target derivation and strict schema/prompt roundtrip;
- all five presentation classes and multiple exact pointer/value selections;
- compact twelve-page admission under the canonical `59,904` limit;
- atomic success plus missing, extra, duplicate, reordered, stale, illegal-pair,
  cover, prop, page, action-coverage and non-target drift rejection;
- input non-mutation on every failure;
- `spatial -> v4 -> candidate` in three calls/two repairs;
- safe presentation-first split when a follow-up slot exists;
- no final-slot provider dispatch for an inadmissible mixed v4 route;
- a receipt/readiness terminal with two actual calls and no invented third one;
- rejection of re-digested route accounting, attempt/mode identity drift, v32
  receipt injection, terminal mismatch and source/request cross-binding;
- current version bindings, immediate legacy classification, child authority
  v1 and QA bridge v2; and
- bridge replay through the current Supervisor/Fresh/candidate chain.

## Validation

- Unique focused/adjacent set: **13 files / 539 tests PASS**.
- `npx --no-install tsc --noEmit`: PASS.
- `npm run story:autonomous-typecheck`: PASS through the literal repository gate.
- `git diff --check`: PASS.
- Internal read-only adversarial review: no production blocker after fixes.
- Final micro re-gate: **114/114 PASS**; re-digested attempt/mode drift rejects.
- One literal `npm run check`, exactly once and without retry:
  - ordinary: **262 files passed / 16 skipped / 4 failed**;
  - ordinary assertions: **3,236 passed / 65 skipped / 5 failed**;
  - resource-intensive: **20 files / 599 tests PASS**;
  - both diagnostic protocols valid; no timeout, RPC/IPC, reporter, launch,
    signal, teardown or other infrastructure failure.

The five failures are the established missing ignored-output fixture HOLD only:

- `child-lexicon-ages-5-8.spec.ts` — one missing story fixture;
- `momentum-gate-koko.spec.ts` — one missing page-beats fixture;
- `page-entity-qa.spec.ts` — one missing PNG fixture; and
- `story-read-back-validation.spec.ts` — two missing story fixtures.

They are unchanged and not waived.

## Independent Claude Code QA

Claude Code independently reviewed exact immutable range
`f669a192835ea1a36ec15dbd2310ce861901d39a..c73c22d6a2425bade902103566efba6a1468debc`
read-only and returned **PASS** with **0 BLOCKER / 0 MAJOR / 0 MINOR**.
It confirmed the atomic v4 authority and applier, exact selected-`mustShow`
pointer/value invariant, three-call route, final-slot no-dispatch behavior,
receipt/readiness cross-bindings, minimal version cascade, unchanged policy and
cost boundaries, and material regression coverage. The five ignored-output
fixture failures remain the separate release HOLD and were not waived.

The reviewed implementation commit was pushed to
`origin/codex/r1d-atomic-compact-book-surface-v4`. This technical PASS and push
do not authorize Fresh Readiness, provider spend, render, deployment,
production or product acceptance by themselves.

## Boundaries and rollback

No model, tier, reasoning, standard call/repair budget, output caps, timeout,
transport retry, fallback, terminal reference-cleanup policy, hard cost ceiling,
candidate semantics, Blueprint, Wizard, render or production behavior changed.
No credential, network, provider, Fresh Readiness, live authoring, image, Vision,
render, storage/database, deployment, production or push action occurred.

Rollback is the focused revert of the implementation commit. Historical
artifacts require no migration or rewrite. Any paid boundary still requires a
new Fresh Readiness authority bound to the exact pushed head.
