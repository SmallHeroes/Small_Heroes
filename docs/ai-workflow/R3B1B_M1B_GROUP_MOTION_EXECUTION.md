# M1b — group/motion continuation

2026-09-07. Guy instructed continued implementation without repeated approvals.
The approved general semantic-recovery Decision Gate remains controlling.
This task is sole writer on `codex/r3b1b-semantic-recovery-m1` in
`C:/GNart/Work/sh-r3b1b-semantic-m1`, starting at `fe07a65e` (clean, cached ahead 4).
d53b `768ccb2f` and accepted-intent `63ccb484` remain clean read-only dependencies.
Claude's supplied PASS P0=0/P1=0/P2=0 covers `d3e6bf7f..fe07a65e`; the SHA-256 of
the delivered full-green log matches. That review did not rerun the full check
or cover diagnostic commit `d3e6bf7f`. No independent PASS is claimed for new work.

## Observed / expected / root cause

Reviewed supporting cast already distinguishes individuals, human groups and
non-humans. The compiler deliberately rejects groups; all downstream cast
inventories currently enumerate only hero, companion and individual humans.
Representing an ensemble as an individual would fabricate identity, appearance
and cardinality. Separately, the current v3 action catalog has no `runs`.

First implement the bounded group/schema path, then settle the catalog cutover
against the frozen v3/v9 replay. Human groups use a separate `humanGroups`
collection, explicit `multiple_unspecified` cardinality, recurring-ensemble
membership and a versioned non-family appearance-continuity policy. No member
count, gender or individual roster is synthesized. Compiler-owned groups come
only from reviewed source entries, never provider draft humans/prose.

Group-bearing templates explicitly use `vc-schema/v5`; legacy no-group templates
retain v4 and their exact serialized shape. Materialization carries group
authority unchanged; validators enforce identity/presence in both directions,
and prompt/Blueprint/runtime consumers project multiple humans. No empty
collection is injected into old artifacts. Non-human compiler support remains
closed rather than converting arbitrary species into humans.

## Consumer inventory and order

Types and source-fact assembly; compiler presence/cover/fact guards; base/vNext,
template and resolved validators; materialization and adapters; prose/prompt,
review and repair inventories; Blueprint provider wire/reference validation;
runtime authority/projection and source-reconciliation protected fields.
Each listed consumer will be changed or explicitly regression-tested. Existing
individual appearance/family policies stay intact. Catalog migration follows
only after examining all current-version consumers and exact replay behavior.

## Stop-check / validation / rollback

General system change, no story/role/page branches. Production consumers are
affected, so test representative recurring groups, individuals, absent groups,
wrong classifications, injected counts/gender/family fields, collisions,
schema downgrade, missing/wrong-page membership and old no-group artifacts.
Test actual compiler-to-prompt consumers, both typechecks and full repository
check; retain original P1 raw inventory and exact no-provider replay evidence.
No product choice is unresolved for this source-faithful representation.
No generated image is needed for this structural milestone. Existing owner
cover/cart choices remain binding for later M2, not applied here by implication.
Focused local commit(s), explicit staging, no automatic push; reversible by
reverting new code, never rewriting/removing original candidate or receipt.
External spend $0; no credential, provider, render, publication, Blueprint
approval, package promotion, payment, deployment or worktree cleanup.
P1 remains HELD 0/3/3 until actual semantic recovery and independent review.

## Delivered slice and explicit limits

This focused checkpoint implements the **group foundation only**, not completed
M1b. Catalog v3, candidate v9 and the compiler's old no-group prompt/schema policy
remain unchanged. The next code slice must add `runs` with an explicit historical
v3 replay lane; changing a global current catalog without preserving compiler
prompt/schema/accounting identity is rejected. M2 atomic recovery is not started.

The group policy represents one recurring ensemble identity containing multiple
unspecified humans. It deliberately does not define a member roster, count, gender
or family-linked palette. Runtime projection includes only frame-present group
descriptions/policy, not whole source-evidence quotes. Concrete visual likeness
between rendered ensemble members still needs later visual review; no image or
product acceptance is claimed here.

Reviewed facts own every group field. Provider-drafted groups are removed when
there is no reviewed group authority, or overwritten with the reviewed facts
before reference/repair routing. The legacy direct compiler has no review input
and explicitly rejects group output. Template draft JSON schema stays unchanged:
groups are compiler-owned, not a new provider-controlled field. Candidate factory
and persistence continue rejecting reviewed-cast previews. Existing cover-only
correction/bridge and historical candidate readers are unchanged.

Consumer dispositions: individual appearance/materialization policy is unchanged;
group presence is separate and bidirectional. Repair inventories include the
compiler-owned group IDs without changing repair budgets. Reconciliation's
current global citation roots include groups only when present (old review bytes
unchanged). Blueprint and runtime cast inventories include group IDs; world and
appearance digests bind full group authority. Group renderer projections never
read family appearance. Detection entries remain ephemeral, not persisted anchors.

## Validation evidence

- Delivered focused run: **10 files, 332 tests passed**, exit 0, 14.07 seconds.
  `outputs/qa-m1b-groups-20260907/focused-delivered.log`, SHA-256
  `7ffb7346a88d1fd3136977fa9d23164091b335e199a795faf269a866f1571ca0`.
- Positive paths: recurring ensembles across arbitrary workshop/harbor sources;
  actual accepted-source compiler -> template v5 -> materializer -> resolved
  validation -> prompt/PVB facts; actual Blueprint wire/validation and bound
  runtime cover/page frames. Family substitution leaves ensemble data unchanged.
- Negative paths: 32 template/resolved cases reject malformed/empty groups,
  schema downgrade, absent v5 authority, fabricated count/gender/family traits,
  identity/alias collisions, wrong/duplicate/missing page membership and individual
  encoding. Source-input/digest substitution and unsupported non-human rejection
  stay tested. Forged provider group data cannot become authority; changed group
  facts change the world digest and fail the compiler fact guard.
- Original P1: 14 files, 412516 bytes, raw inventory
  `cd621f7712912a92d14fa87432c609c5e92a006363e7eed7be5231a856e8fdd0`.
- Delivered active-code held replay: exit 0, `providerCalls: 0`, exact captured
  call sequence and receipt/candidate outcome congruence, rebuilt candidate
  identical. Template digest
  `8632bd95869b93bf115500f3a24890daaa967a39805079e2f0ae86afc2543b18`,
  candidate digest
  `efbdd2e13c03af194af425c5e050e0bfedad29d4e0c8cec1aa8b54530192ad88`.
  `held-replay-delivered.log` SHA-256
  `7a3f2dad52097c8d20b7f8764d7f12a84b3c982c3304eddda10f3efce40230cc`.

The first full run (`full-check.log`) is **not delivery evidence**: it ran during
the legacy-boundary follow-up and had one ordinary-test failure (historical Fox
validator error wording), 4921 ordinary passes/73 skips, and all 671 resource
tests passed. Exact historical error wording was restored; the dedicated Fox,
legacy-compiler and template regression set then passed 128/128. No historical
fixture pin was changed and no test/runner/deadline was disabled or relaxed.
Early new-fixture/diagnostic-constructor errors were corrected, not relabeled
inherited. Final full-run result is recorded below after completion.

The second attempt (`full-check-delivered.log`, despite that provisional name)
is also **not a green delivery**. Its ordinary phase had 4922 passes / 1 failure /
73 skips: the AST producer census correctly found four legacy compiler rejection
sites after the new source-review guard, versus the prior expected three. Only
that exact count was updated (3 -> 4); all typed-argument/exhaustiveness assertions
remain. The census + legacy-compiler run then passed 36/36, exit 0, log
`producer-census.log` SHA-256
`9e20d481495585b7397d5bc290c7540e62779ee70797d456106b30d968c132eb`.
Its resource phase passed 670/671 with one 5000ms timeout in
`live-execution-request-materialization.spec.ts`, the real public entry sentinel
test. The same test passed in the first attempt. No root-cause/inheritance claim
is made from that difference; no timeout, worker count or runner is changed.
The final full check uses the complete corrected code/test tree, a new log
`full-check-final.log`, unchanged 4/2 workers and no parallel test commands.

## Independent review targets

### Final local result

`npm run check` **exit 0** on the delivered code/test tree. Root
`tsc --noEmit` and `tsc -p tsconfig.story-autonomous.json` both passed.
Ordinary phase: 347 passed / 17 skipped files, **4923 passed / 73 skipped tests**,
119014ms including phase overhead. Resource phase: 22 passed files,
**671 passed / 0 skipped tests**, 302501ms including phase overhead. Total
**5594 passed / 73 skipped / 0 failed**. Canonical inventory 386/364/22,
unchanged 4/2 workers; one execution of each phase in this final invocation.
Both phase diagnostics and overall gate passed, with empty failure classes.
The previously timing-out public-entry sentinel test passed unchanged here;
this does not establish that timing variability is permanently resolved.

Final full log: `outputs/qa-m1b-groups-20260907/full-check-final.log`, SHA-256
`57695284dd7d15892204f837d612ddfcd1112b263bb6bc8f545fff4908f69a62`.
No code/test changes followed that run. Documentation was finalized afterwards.
Local validation is not Claude's independent PASS. Source/candidate artifact and
receipt authority remain held and unchanged. No external spend or automatic push.

### Handoff

Review the delivery commit from parent `fe07a65e`, read-only. Try to falsify:
source authority cannot be fabricated through either compiler; ensembles cannot
be collapsed into individuals; all present/absent/cover/page consumers preserve
the separate identity; digests bind group changes; invalid group input fails
without a runtime throw; old no-group fixtures and held v9 replay are byte-exact;
new data cannot acquire candidate/package/render/product authority by implication.
Inspect reference-repair and reconciliation paths, not only happy-path tests.
No group-specific live repair attempt or paid visual sample was executed.
