# General creative replacement v4 predecessor adapter

Guy authorized source execution and explicitly requested other reviewing agents
while Claude is paused. Same task/worktree sole writer: sh-r3b1b-semantic-m1,
codex/r3b1b-semantic-recovery-m1 at39c528a4, clean/ahead9; dependencies768ccb2f/
63ccb484 clean/read-only. Two delegated agents are read-only: editorial review
of exact225f2b01... candidate and technical adapter investigation. Their verdicts
are attributed to those agents, never rebranded as Claude Code PASS.

Observed: sync CJS creative-replacement loadPredecessor recognizes only v2 and
creative-v1, while current source is v4. Full shared TS v4 authority validation
already exists. Installed tsx/cjs/api exposes synchronous require and successfully
loads that exact validator without adding dependencies or a subprocess.

Proposed smallest change: a v4 branch in the common predecessor loader invokes
the real shared validator, then cross-checks revision and raw manifest identities.
Both publish/input validation and self-contained accepted successor reload use
that loader. Keep old v2/creative-v1 behavior unchanged. Do not accept v4 by schema
label alone, fork from older ancestors or copy old directions into revised text.

Scope: one CJS lifecycle, existing focused test file, documentation. No new
schema/version, runtime selector, provider, timeout, dependency or image change.
Risk: CJS/TS bridge path resolution and forged/incomplete predecessor authority.
Verify ordinary Node CLI and library calls, root isolation, failed/tampered input,
reload after staging removal, old-path compatibility and collision/fork behavior.
No migration of existing artifacts; rollback is focused commit revert.

Acceptance: copied real v4 predecessor verifies; missing/tampered files or
approval/identity fail before writes; self-contained reload repeats verification;
old tests remain green; relevant adjacent tests and tsc pass. Run full repository
check and report actual outcome without waiving previous stability failures.
Agents independently challenge completed immutable code after implementation.

Stop-check: general source-lifecycle fix, no new creative direction, no image to
eyeball, zero provider/render allowance. Guy's source wording acceptance remains
valid; independent editorial review must be real and separately attributed. No
accepted publication before review prerequisites are satisfied. Existing exact
editorial-review-byte acceptance is not silently invented. No pushes/deployment.

## Implementation checkpoint

Technical reviewing agent confirmed a cwd-dependent alias bug in the naive
bridge. Six import/re-export specifiers across3 shared TS files now use relative
paths: acceptedStorySourceAuthoringAuthority.ts, integrity.ts and
story-validators/utils.ts. No validator logic changes in those files. The CJS v4
branch runs full validation and binds raw manifest SHA, canonical manifest digest
and revision identity; old branches unchanged. Same branch covers successor reload.
No new fork semantics: existing creative-successor collision handling retained;
it is not a claim of global latest-revision authority or multiwriter locking.

Initial regression before code:3 failures/9 passes. After final tests:90/90 across
8 focused/adjacent specs, including13 lifecycle cases. Tests cover copied real
v4 authority, preview/publication/idempotent replay, reload without staging,
tampered/missing source/review/acceptance/identity, rehashed authority drift,
hardlinks, fork rejection and plain Node from a foreign cwd with hostile aliases.
All test publication is isolated fixture data; no real source published here.
Full repository check started; result pending, not claimed green.

Delegated editorial reviewer independently returned pass on exact225f2b01...,
no prose revisions. Separate attribution and exact returned JSON are stored under
outputs/dini-story-rewrite-20260913/execution-01/. Not Claude Code PASS.
