# R1D Accepted-v3 Visual Contract Authoring and Wizard Cutover — Decision Gate

**Date:** 2026-08-24

**Status:** approved for provider-free materialization and proof; one canonical
live authoring attempt is authorized only after the offline evidence receives
independent Claude Code PASS

**Owner authority:** Guy explicitly authorized continued work until the Wizard
can create a complete book from the new story. Guy's later restriction remains
controlling: this gate grants no image, Vision, audio or book-render authority.

## 1. Proposed change

Carry the exact product-accepted Story Source revision
`3ef645415b3cdd5945baeaa275d97ae0aa0491bf30addbcc46208475278f534a`
through the existing canonical Visual Contract authoring authority, then—only
after a valid Candidate and the repository's existing approvals—author a fresh
Blueprint, assemble a fresh Visual Package and deliberately advance the
Chameleon current locator.

The work is split into closed milestones:

1. provider-free accepted-v3 binding, canonical live-request materialization,
   verification and offline harness proof;
2. at most one canonical live Visual Contract authoring attempt under the
   unchanged policy and budget;
3. Candidate/reconciliation review and approval;
4. fresh Blueprint review and approval;
5. fresh Visual Package review and approval;
6. current-locator promotion and provider-free Wizard/runtime verification.

No later milestone inherits authority from an earlier technical PASS. Exact
human approval artifacts remain required wherever the existing lifecycle
requires them.

## 2. Why now?

The new Story Source and Visual Directions are accepted and canonically
published, but deliberately runtime-ineligible with reason
`accepted_story_source_requires_fresh_visual_contract`. The current locator
still selects package `2b488f2d...98a6`, whose Story Source and Visual Contract
describe the superseded bus-stop story. Reusing or rebinding that contract
would create cross-story authority drift.

The shortest honest route to an operational Wizard is therefore a fresh
Visual Contract from the accepted v3 source, not another story migration or a
locator shortcut.

## 3. Scope

- General existing canonical authoring path, not a story-specific compiler.
- One exact accepted-v3 source instance for the real proof.
- Provider-free materialization and harness evidence first.
- One paid text-authoring attempt only after independent offline QA.
- Later Blueprint/package/locator artifacts remain separate immutable gates.

## 4. Risk of hardcoding

No production branch may name Bar, Kim, kindergarten, lantern, page numbers or
the real revision digest. Real identities belong only in repository-owned
request/evidence artifacts. Any required bridge must accept a validated generic
accepted-revision manifest and derive story identity, source path and
preservation inventory from it.

## 5. Files likely affected

The offline milestone may affect only:

- a general accepted-v3 authoring binder if the existing interfaces cannot
  carry the full accepted authority safely;
- focused tests and workload inventory when code changes;
- ignored canonical materialization/readiness artifacts under `outputs/`;
- `CURRENT.md` and this milestone's implementation evidence.

The accepted revision bytes, current Visual Package, current locator, Wizard,
Boards and renderer must not change during the offline milestone.

## 6. Expected behavior after change

- The authoring source is exactly the published v3 `integrated.md` and its
  nine-file accepted inventory is revalidated and preserved.
- The canonical source snapshot parses all eight pages and exact Visual
  Directions with no provider access.
- Materialized request versions, schema compatibility, policy, call schedule,
  model, pricing and projected cost all match current repository authority.
- Offline verification and the repair harness fail closed on any source,
  manifest, acceptance, path, digest or inventory drift.
- A future live attempt cannot mutate the accepted source or current runtime
  authority and cannot silently retry, resample or fall back.
- A successful Candidate still cannot reach Blueprint/package/Wizard without
  the existing exact review and approval chain.

## 7. Validation plan

1. Recompute the v3 manifest and all nine file descriptors.
2. Build and validate the exact source snapshot from published `integrated.md`.
3. Materialize and verify the canonical live request at `$0`.
4. Run the existing offline repair harness with no provider.
5. Prove the v3 manifest, accepted directory, current locator and four Board
   artifacts remain byte-identical.
6. Run focused authoring/materialization/accepted-source/runtime tests,
   TypeScript and diff hygiene.
7. Obtain Claude Code PASS before any credential or provider boundary.
8. If live authoring is later executed, stop after that one attempt and inspect
   its sanitized receipt before any further action.

No page or book image is required to prove this milestone.

## 8. Cost impact

- Offline/materialization milestone: exactly `$0`.
- Later live milestone: at most one canonical authoring execution under the
  unchanged hard ceiling of `$10`, model `gpt-5.6-sol`, medium reasoning,
  current call schedule, zero transport retries and no fallback.
- No image, Vision, audio or render spending.
- No second paid attempt is implied. A failed attempt triggers diagnosis and a
  new evidence-based decision, not symptom patching or automatic rerun.

## 9. Rollback plan

Before live authoring, remove only the new ignored materialization/output root
and revert any focused generic binder commit. The published accepted revision
and current locator remain unchanged. After a later successful authoring call,
artifacts are immutable evidence and are never deleted or rewritten; rollback
means declining promotion and leaving the current package selected.

## 10. Review assignment

- Guy has authorized the non-render chain to an operational Wizard and retains
  product acceptance of Candidate, Blueprint, package and final visual result.
- Claude Code first falsifies offline accepted-v3 binding, canonical request
  versions, cost/call policy, path containment, preservation and provider
  unreachability.
- After a live attempt, Claude Code falsifies receipt/call fidelity, Candidate
  validity and absence of retries/fallback/downstream mutation.
- Claude Cowork is unnecessary for the offline technical boundary. Product or
  creative consultation resumes only if Candidate/Blueprint review exposes a
  story or visual-direction problem.
- Guy should eyeball the Blueprint review/contact sheet before package approval
  and any future image render.

## 11. Stop-check resolution

1. General route: yes; real story identity is data only.
2. Other stories/styles: protected by unchanged validators, schemas and
   content-addressed paths; focused regression covers existing consumers.
3. Production behavior: unchanged until a separately reviewed locator move.
4. Money: `$0` now; at most `$10` later under the existing fence.
5. Smallest proof: source snapshot + canonical materialization + offline
   harness, no provider.
6. Guy decision: continued work to operational Wizard is explicit; render is
   explicitly excluded.
7. Claude targets: v3 authority binding, version freshness, cost/call caps,
   mutation/retry/fallback absence and runtime inertness.
8. Claude Cowork: not needed for the technical/offline step.
9. Guy eyeball: Blueprint planning review before package or image work.

## 12. Do not do

- Do not reuse, patch or rebind the superseded bus-stop Visual Contract.
- Do not move the current locator before a new package is approved.
- Do not call a provider before offline evidence and Claude Code PASS.
- Do not change model, pricing, budget, retries, fallback or call counts.
- Do not run more than one live authoring attempt under this gate.
- Do not render an image, page, cover, contact image, audio asset or full book.
- Do not mutate the accepted v3 revision or the four untracked Board artifacts.
- Do not treat a Candidate, technical PASS or Blueprint as product/package
  approval.
