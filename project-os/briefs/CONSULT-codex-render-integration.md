# Codex consult — verify END-TO-END integration BEFORE the render proof (does an engine-authored template render correctly?)

## Why now
The offline text-first authoring engine is complete + proven: batch-minted **all 18 slots → 18/18 valid** (candidates in `_review/vc-live-cheap`, gpt-5.5). Before we promote one template into the render bank and spend image budget on a staging render, verify the wiring is coherent end-to-end. **Authoring is not in question — the render-path INTEGRATION is.**

## The specific risk (please nail this first)
Two branches, and neither has everything:
- **Engine** lives on `feat/live-authoring-fix` (7 commits off base `867c080d`). It MODIFIED shared modules the render path also depends on — at least: `lib/visual-contract-compiler/contractTemplateTypes.ts` (**`RELATIVE_ROLES` now includes `parent`**), `validateTemplateContract.ts`, `appearancePalette.ts` / `appearanceBindingCoherence.ts`, `assertSourceProse.ts` (new), `templateDraftSchema.ts` (new), `lib/story-validators/parser.ts`, `backend/providers/pipeline.ts` (`LLMCallOptions` + `json_schema`). It also added S2a appearance-injection, S2b topology canonicalization, and a Stage-3 repair loop — all in the compiler, but they SHAPE the template output.
- **Render path** (freeze/materialize/enforce: `ensure-frozen-visual-contract.ts`, `materializeContract.ts`, `validateResolvedContract.ts`, `contractRenderGuards.ts`) lives on `feat/chunked-generation`, which branched/moved independently and did NOT receive the engine's shared-module changes.

**Question 1 (the crux):** If we promote an engine-authored `fox_uri_adventure.visual-contract-template.json` (from `_review/vc-live-cheap`) into `story-bank/v3-approved/` on **`feat/chunked-generation`** and render it on staging — does it materialize + freeze + render **correctly**, or does the render branch's older materializer/validator/types produce a wrong or degraded result (or reject it)? Concretely: are there template fields/shapes the engine now emits (appearance binding origins, `paletteId`, zones+`stableGeometry`, topology `zoneId`/transitions, any schema/version bump) that `feat/chunked-generation`'s materializer/validators don't understand or resolve differently? **Which branch is the coherent one to render from — feat/chunked-generation (render path, old schema) or feat/live-authoring-fix (new schema + render path @867c080d but behind on other feat work)?** If shared-module changes must be ported to the render branch first, name exactly which.

## Also verify
- **Q2 — materialize→resolved validity:** the engine template passes `validateBookVisualContractTemplate`. After per-order materialization does it produce a `ResolvedBookVisualContract` that passes the stricter **resolved** validator (`validateResolvedContract.ts`) and freezes? The unresolved appearance bindings (`deterministic_palette` / `family_profile`, no value; `hairStyle` explicit) must resolve, not reject.
- **Q3 — paletteId semantics:** S2a writes `origin.paletteId = cast id`, but `appearancePalette.ts` selects by `hash(schemaVersion|paletteVersion|storyKey|castId)` and (per your earlier note) does NOT read `origin.paletteId`. Is that a latent inconsistency at materialize, or harmless/cosmetic? Any version mismatch (`schemaVersion` / `PALETTE_VERSION` / `policyVersion`) between engine output and render materializer?
- **Q4 — fail-closed end-to-end:** confirm nothing lets a mismatched/invalid engine template render silently or lets enforcement no-op (VERCEL_ENV/preview gate, freeze-before-hash validate, render-gate). Enforcement is meant hard-off on prod, active on staging/preview.
- **Q5 — the `parent` role:** does classifying `parent` as a relative (family_profile) on the engine branch, but NOT on the render branch, cause any materialize/resolve divergence for a story whose extractor emitted `parent`?

## Deliverable
A go / no-go on the render proof as planned (promote fox template to feat/chunked-generation + staging flags + 5-page sample), plus the exact list of any shared-module changes that must accompany the template asset, and which branch to render from. Cite files:lines. Read-only; no changes.
