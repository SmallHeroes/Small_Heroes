# CODEX CONSULT — the cutover plan you approved rested on a false premise supplied by Cowork

## 1. ROUTING + TARGET
- **Reviewer:** Codex. **Mode:** design review + ruling. Read-only.
- **Subject:** the Production Reconciliation Bridge plan you issued and subsequently gated (Tracks 1.2, 1.3, 2), now **cancelled**.
- **Why you are being told:** you approved that plan, and issued three gate verdicts on work derived from it, **on data Cowork supplied that was wrong**. You could not have caught it — you audited structure and code, never data provenance.

## 2. THE ERROR
Cowork measured the `ozxjmnzybzetqudivlbw` Supabase project — 244 orders, 190 books, 1,540 pages, 1,187 image assets, 70 payment records — and presented those figures to you as **live production data at risk**. Every downstream decision followed from that: preserve the data, therefore bridge rather than rebuild; therefore prove schema equality across nine dimensions; therefore two clone rehearsals; therefore quarantine the 19 non-terminal orders; therefore repair the migration history rather than start clean.

**Cowork never asked whether that data was real.** The question "244 orders" versus "244 *customer* orders" was one query and one question away, and was asked only when Guy volunteered the answer unprompted.

## 3. THE VERIFIED FACTS (confirmed by query and by Guy)
- The project has **never been live**. Orders stop at **2026-06-18** — the exact day the staging project was created. It was the original *development* database that later received the label "production". Its Supabase name was the autogenerated `guynagar21@gmail.com's Project`.
- **No customer payment has ever occurred.** 67 `PaymentRecord` rows use provider `fake` (test renders). 2 Stripe rows (₪158 total, both 2026-04-22) are self-tests — and **Stripe is not the payment rail for Israel**; PayMe is.
- **No rendered book needs to remain accessible.** Everything of product value was rendered on QA/staging.
- The 14 residual child photos in that bucket were **development uploads**, not customer uploads — which also corrects Cowork's earlier framing of that finding's severity to you.

## 4. WHAT WAS CANCELLED, WHAT SURVIVES
**Cancelled:** the bridge (`build-bridge`), the quarantine design and script, the migration-history repair (the coupon 25%→50% restore + corrective migration), and both clone rehearsals.

**Survives — unaffected, because it is product code and safety mechanism, not data plumbing:**
- The **delivery fence** — `deliveryFenceVersion`, the `order-authority.ts` funnel, marker precedence, the ship/hold/release CAS, receipt keying, the Outbox fence. **Your six rounds there found real P0s** — automatic shipping of a safety-held book, double delivery email, a force-ship window on the path every book takes. All would have been live defects the moment a real customer ordered. That work is the bulk of the effort and it is entirely intact.
- **Human-QA Slice 1**; the **structural funnel guard**; the **real-Postgres harness** (21 tests); the **child-photo deletion fix** (cleanup never ran on order failure — a genuine future-customer leak); the **security work** (Data API disabled, the held RLS/grants migration, the private-bucket direction); the **schema-equality checker** (built for the bridge, retained for environment-parity proof).
- **Your Track-2 P0-1 finding** — that the canonical chain cannot deploy from an empty database — is the only part of Track 2 that survives, and it is now the critical path.

## 5. THE REPLACEMENT PLAN (already in motion)
1. **Baseline `init` migration** prepended to the chain (`00000000000000_init`), derived analytically as *target minus chain-created objects*, plus the missing `migration_lock.toml`. Closes your P0-1.
2. **New production project created clean:** `yevwpjxqusyyaxalbvyn`, **eu-central-1 (Frankfurt)**, Postgres 17.6, verified **0 tables / 0 migrations**. The old Tokyo project is **already deleted** — the 14 dev photos went with it.
3. **Region rationale:** `vercel.json` pinned no region, so functions ran in Vercel's default (Virginia) while the database sat in **Tokyo** — every query crossing the Pacific, dozens per book, never noticed because production never ran. What matters is database-to-function proximity, not database-to-user. Frankfurt for both; `"regions": ["fra1"]` to be pinned.
4. **Security hardened at creation** rather than retrofitted: Data API off, automatic RLS on, and Cowork revoked the default privileges granting `anon`/`authenticated` full access to future tables (verified: only `postgres` and `service_role` remain on tables, functions and sequences).
5. Deploy the chain, prove it, point Vercel at it, one canary order.

## 6. RULE ON
1. **Does the simplified path miss anything the bridge plan covered?** Specifically any *safety* property — not convenience — that reconciliation would have given and a clean build does not.
2. **The baseline approach.** CC prepended an `init` migration rather than squashing the chain. Is prepend correct, and is deriving it as "target minus chain-created objects" sound, given there is no recoverable pre-chain commit (the repo's history was reset — the entire chain landed in one "Initial commit")?
3. **⚠️ Staging is now inconsistent with the chain.** Its `_prisma_migrations` was **hand-seeded by Cowork on 2026-07-18** (rows inserted to mark migrations applied, with the SQL executed separately) — you later ruled that method prohibited for production. It now also lacks the new `init` migration, and `init` emits unguarded `CREATE TABLE`, so `migrate deploy` against staging would fail. **Rule on whether staging must also be rebuilt clean** for true parity, or whether a narrower repair is acceptable.
4. **Verification environment.** Docker will not start on Guy's machine, so CC cannot use a throwaway container. Cowork proposes deploying the chain **directly against the new, empty production project** — arguing it is a *better* test than a bare `postgres:16`, because migration `20260702` performs `REVOKE ... FROM anon, authenticated` and those roles exist only in a real Supabase project. The database is empty and disposable; a failure is reset in seconds. **Acceptable, or do you require an isolated target?**
5. **Anything in this correction that should change how the remaining launch work is gated.**

## 7. OUTPUT
A ruling on each item in §6, and an explicit statement of whether any prior verdict of yours (Track 1.2, 1.3, Track 2) should be treated as void rather than merely superseded — so the record is clean.
