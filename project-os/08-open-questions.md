# 08 — Open Questions

**Last updated:** 2026-07-06
Grouped by area. Each needs an owner decision or a routed brief. Resolved questions move to `02-decision-log`.

---

## Product
- **OQ-P1:** Which exact 8/18 slots are launch-eligible, and what's each one's QA status? (blocks G1/G3)
- **OQ-P2:** Is audio in or out of the sellable MVP promise for 07-15? (drives G5)
- **OQ-P3:** Final honest-copy pass — confirm no "in minutes", no likeness-without-photo promise, clear no-photo limitation.

- **OQ-P4:** [RESOLVED — Guy 2026-07-06] Default DNA for the no-photo mother is **ACCEPTABLE** (no real family to match without a photo). Binding requirement: she must look **consistent across the book** (locked appearance) AND **wardrobe follows time/scene logic** — same time/scene → same clothes; a time jump (night → next day) → different clothes. So keep the deterministic default-DNA path; open work = verify/enforce intra-book appearance consistency + time-aware wardrobe. NOT a bunny-pilot blocker (bunny is single-scene-time); matters at scaling. Engine support for per-time wardrobe variation = flag for the scaling phase.

## Technical
- **OQ-T1:** [ROUTED → Cursor, doc-only brief] Repo role docs conflict — `AGENTS.md` names **Codex** as CTO; `CLAUDE.md` + `AI_ROLES_AND_PROTOCOL.md` name **Claude** as CTO. DEC-008 accepted 2026-07-06; reconciliation brief at `project-os/briefs/BRIEF-cursor-role-doc-reconcile.md`. Close when merged.
- **OQ-T2:** Codex round-2 = FAIL → Claude Code fixed all 3 seams, pushed `32dcfe3a` (green 1333/15). **Now awaiting Codex round-3 re-gate.** CC's verify-list for Codex: no hash regression (materializeContract untouched); Fix-2 resolved-shape detection keys on materializerVersion/paletteVersion; the one retargeted dispatch test (tightening not weakening); Fix-3 mirrors Template invariants. Close when Codex verdict in.
- **OQ-T6:** Push governance — Claude Code pushed `32dcfe3a` itself (clean fast-forward, DoD required it), which conflicts with `03-agent-roles` "only Guy pushes". **Guy to decide:** push stays Guy-only, OR executors may fast-forward-push feature branches (never main/prod). Recommendation: allow FF-push on feat, Guy-only for main/prod.
- **OQ-T5:** [RESOLVED 2026-07-06 — Codex PASS] `fc1ca72d` Codex-PASS, zero findings (verdict from the independent Codex CLI — CC did NOT self-certify its own commit; the model worked). Render-gate laundering bypass closed via shared `classifyFrozenContract`. **Engine now fail-closed at BOTH the freeze seam (P0) and the render gate (P1).** Enforcement still OFF everywhere. Go-decision → OQ-T7. Note-for-later: invalid-contract failure classified retryable/infra_transient (bounded 3) — arguably should be permanent-fail.
- **OQ-T7:** [AUDIT DONE — see `09-staging-enforcement-enablement.md`] CC audit: **0 templates, 1 contract (`bunny_ometz_adventure`) repo-wide.** With `ENABLE_V3_APPROVED_BANK=true` all 18 slots serve from `v3-approved`; freeze looks there → **1 slot renders, 17 block (7/8 sellable block).** **NO-GO for broad enablement.** Recommended (CC): a **narrow mechanism-validation run** on staging — 1 bunny-adventure order (render passes) + 1 gap-slot order (block works) — to prove plumbing without pretending the catalog is ready. Flags: `VISUAL_CONTRACT_ENFORCEMENT` + `VISUAL_CONTRACT_FREEZE` + `ENABLE_V3_APPROVED_BANK`, staging `VERCEL_ENV=preview` (prod is hard-off), redeploy, rollback = flags off + redeploy. Real blocker → OQ-T8. ⚠️ Correction (Codex): a legacy-bunny run is a PLUMBING test only — it never materializes and uses the legacy validator; the meaningful pilot needs a Template + `VISUAL_CONTRACT_STEERING=true`.
- **OQ-T8:** [PILOT Codex-PASS 2026-07-06 — staging audition is the next gate (Guy go-decision)] bunny Template authored (`d1d724ca`, +526, green 1345/15) — FIRST Template in repo, Codex-verified end-to-end offline (no defects); materializes to valid `contractKind:"resolved"` on BOTH photo + no-photo orders (offline, no render), deterministic/hash-stable; doctor family_profile bug FIXED (→deterministic_palette). Findings: **F1** no-photo → mother derived from DEFAULT/invented child DNA (fallback olive-skin/brown-hair) — materializes fine but not the real family → product decision (OQ-P4); **F2** NO structured multi-zone geometry lock — bunny (2 loc/3 zones) rides on free-text zone.description (= legacy, no regression, but a real gap for cross-zone consistency locking; the audition will test it via text only). Also: `project-os/` is GITIGNORED — commit OS with `git add -f`. Audition-1 (pages 4+9, LOW): art + child-likeness + companion(bunny)-lock all consistent (good); **INCONCLUSIVE on recurring HUMANS** — 4+9 don't co-contain mother+doctor (p9=night, no doctor, parent rendered father-like ≠ p4 mother); child shirt gained a sun graphic (possible wardrobe drift). ⚠️ CC diagnosis (code-grounded): **Audition-1 ran with STEERING OFF** → it exercised the LEGACY path, not the engine (so it proved nothing about the Template). Proof: 4+9 BOTH bind mother+doctor structurally (`pagesPresent`), but the p9 father-drift + night-drift are the exact signature of steering-off — the structural human lock (`contractPageSupportingCharacters`, `adapters.ts:330`, gated at `chunk-runner:1237`) was dormant, so render fell to legacy `detectHumanFamilyRolesOnPage` regex which matches `אמא`(p4) but NOT generic `הוֹרֶה`(p9) → parent unbound → father. NOT a Template/code bug — expected with steering off; the lock is structural (not text) so it should hold the mother on p9/p10 WHEN steering is on. **Re-audition: pages 4+10 (clearest mother+doctor overlap), with FREEZE+ENFORCEMENT+STEERING genuinely ON and CONFIRMED in log.** Likely reason steering was off: VERCEL_ENV=production (hard-off) or the order was frozen before the redeploy took effect. If the father recurs WITH steering confirmed on → real steering bug (separate brief). **WS0c artifact authoring.**
- **OQ-T9:** [APPROVED to build — Guy 2026-07-06, from WS0c F2] Build the structured **multi-zone / multi-location set-topology geometry lock** (`adapters.ts:107` emits a structured lock only for 1-location/1-zone books; per-zone `stableGeometry` = `[]`). Core to cross-zone visual consistency (#1 blocker). **Cowork sequencing recommendation (confirm): build it AFTER the bunny full-book audition** — the 2-page audition (4+9) tests same-scene identity; the FULL book tests cross-zone. Build against real drift evidence, not speculatively. Queued engine task (Claude Code) → Codex gate.
- **OQ-T10:** [OPEN — pre-existing reliability bug, blocks re-audition on PHOTO orders] CC diagnosis: re-audition order timed out (`FUNCTION_INVOCATION_TIMEOUT`, `/api/generate/worker` @300s) BEFORE the child anchor — an **unbounded LLM/vision fetch** in the text stage (child-photo vision `describeChildFromPhoto` sonnet-4→gpt-4o; DNA support calls) has NO AbortSignal, so a slow/hung provider eats the full 300s. **NOT caused by enforcement/steering** (they run after that point). Fix (CC proposal → needs Guy approval → Codex): add `AbortSignal.timeout(~30-45s)` + fallback to the pre-image LLM/vision fetches; optionally precompute photo-vision at order creation. Do NOT raise maxDuration (it's a hang, not slow). **Immediate unblock: run the re-audition with a NO-PHOTO order** (skips the vision call) IF the hang was vision. Rule out Preview-Protection 401 on the worker self-call (check `GenerationJob.lastChainStatus`; if 401 → set `VERCEL_AUTOMATION_BYPASS_SECRET`). **FIX IMPLEMENTED `b4b0b1b7`** (green 1347/15): shared `preImageLlmFetch()` = fetch + `AbortSignal.timeout(30s)` on all 9 provider fetches (incl. a 5th, the DNA build, beyond the 4 briefed); hung provider aborts→existing fallback fires; happy-path byte-unchanged; maxDuration untouched; worst-case stack ~210s<300s. **Codex PASS `b4b0b1b7` (2026-07-06)** — GO for staging redeploy + resume re-audition (Codex confirmed: all 9 fetches LLM/vision only, no signal override, worst-case ~210s<300s, fallbacks/happy-path unchanged). Caveat: keep `PRE_IMAGE_LLM_TIMEOUT_MS` unset/default (30s) — the ~210s guarantee holds only at 30s, not arbitrary overrides. **Next: redeploy staging with `b4b0b1b7` → re-audition (photo now safe too).** ⚠️ current staging deploy (d1d724c) lacks the fix. 0 Templates exist; only bunny has a LEGACY contract. The #1 visual-consistency blocker is UNSOLVED for the catalog until per-slot Templates are authored — the engine is only the mechanism. **DECISION B+ (Codex + Cowork, 2026-07-06): prove ONE Template→Resolved→STEERED book (bunny) before authoring the set.** Pilot plan: (1) author `bunny_ometz_adventure.visual-contract-template.json` — reuse the structural spine, replace legacy human prose with structured bindings, and FIX the current binding bug (doctor is non-relative → `deterministic_palette`, NOT `family_profile`; mother → `family_profile`; hairStyle + garments explicit); (2) materialize OFFLINE (no render) vs a photo AND a no-photo order → confirm `contractKind:"resolved"`; (3) Codex review; (4) staging: enable `FREEZE` + `ENFORCEMENT` + **`STEERING`** together (steering is REQUIRED — freeze/enforcement only GATE render; steering is what drives contract-based locations/humans/style); (5) LOW 2-page audition, pages 4 + 9 (both have mother+doctor+child+bunny); (6) Guy eyeball → 5-page/full → **only THEN scale authoring to the pinned 8.** Note: compiler drafts ~70-80% structural, but human-binding conversion + approval is manual → a repeatable draft→Template→validate→review workflow is needed before scaling. Precondition for scaling: pin the 8 slots (OQ-P1).
- **OQ-T3:** vNext reconciliation strategy — how to reconcile the ~45 stranded commits on `fix/visual-contract-live-wiring` without blind rebase. (Codex)
- **OQ-T4:** When/how does `feat/chunked-generation` cut over to PROD, and is env parity confirmed?

- **OQ-PA4:** [🚩 LAUNCH BLOCKER — money/trust bug, found by CC 2026-07-06, task_4e23a3db] The wizard SHOWS ₪59/79/99 but every order CHARGES base + ₪38 (audio ₪19 + pdf ₪19 hardcoded `true` in `buildWizardPayload` since 2026-05-30; `computePricing` adds them). Latent only because checkout is waitlisted. Turning on real payments (coupon launch) = a ₪59-shown / ₪97-charged **overcharge** — trust/legal-critical. Product cards advertise audio+PDF as INCLUDED at ₪59/79/99 → Cowork reading: **shown price is TRUE; the +₪38 is the bug → fix by NOT charging the add-ons** so charge==shown==₪59 (coupon → ₪44.25). **DECISION (Guy 2026-07-06): everything is INCLUDED — the add-ons are leftover code, a bug. Fix = remove the +₪38 so charge==shown==₪59/79/99.** Fix routed to CC → Codex gate. Do NOT enable payments/coupon until fixed + Codex PASS. Blocks ALL real payment + coupon go-live. **FIXED on `fix/remove-addon-charges` @ `dff4681f`** (off feat/coupon-code, pushed, green 1361/19): `computePricing` returns flat base (addonsPrice:0); features still generate, just not charged. **Scope was BIGGER than ₪38** — fantasy auto-enabled video too → was charging **₪166 vs ₪99 (+₪67)**. Now charge==shown 59/79/99; coupon composes → 44.25/59.25/74.25. NOT self-certified → Codex mandatory. Merge with the coupon slice before payments go live.

## Story / content
- **OQ-S1:** Manual QA capacity per week vs slots needed for launch — real throughput number?
- **OQ-S2:** Any slot with unresolved canon issues remaining (scarf/companion payoff notes)?

## Image generation
- **OQ-I1:** Do the 5 first-render defects all resolve via vNext, or are interim mitigations needed for launch?
- **OQ-I2:** Confirm HIGH vs LOW quality budget for the launch slot set.

## Narration / audio
- **OQ-N1:** [ACTIVE — narration-quality track, Guy 2026-07-06] Fix + upgrade narration: (a) wire `applyTtsAmbiguityNiqqudPass` into prod so Hebrew words (homographs) are pronounced correctly (~16/122 bank files bare); (b) make voices sound very human/natural; (c) improve the MOTHER and FATHER voices specifically. Path: **Codex consult (code-grounded strategy) → ONE CC implementation brief** (niqqud wiring + pronunciation/humanness + mom/dad voice config + a small audition page to listen/iterate) → Codex review. To listen today: the reader plays per-page MP3. Creative voice-casting side → ChatGPT/Guy. **Codex consult 2026-07-06 — TWO findings:** (1) niqqud pass NOT wired into `generatePageAudio` (`audio.ts:231`); (2) ⭐ per-page prod narration does NOT send `voices.ts` `voice_settings` — mom/dad stability/similarity/style tuning is DEAD in prod (only the legacy full-book path applies them). Sequenced plan (deterministic first): wire targeted niqqud into `generatePageAudio` → make per-page `voice_settings` effective (pass them through) → audit the 16 bare bank files for misreads (expand RULES) → generate an A/B audition set → Guy/ChatGPT pick voices (re-casting IDs likely higher-impact than slider moves; sync `voices.ts` + `content.js:504`). Targeted homograph disambiguation > full niqqud; do NOT use SSML/tags for pronunciation. Pronunciation dictionaries for names = optional post-launch.

## Viewer
- **OQ-V1:** Any reader polish items that are launch blockers vs post-MVP?

## Payment / order

- **OQ-PA3:** [APPROVED-in-principle — Guy 2026-07-06] Coupon/promo code feature: one shared code (e.g. `FIRST100`), 25% additional discount on the launch price, hard cap of 100 redemptions, entered at the email/pre-payment step. **MONEY-PATH — Codex review mandatory.** Non-negotiables: (1) discount computed SERVER-SIDE (never trust client price); (2) 100-cap enforced ATOMICALLY (atomic `UPDATE ... WHERE count < max`; no race past 100); (3) redemption counted on PAYMENT SUCCESS, not on apply (abandoned carts must not burn the cap); (4) the 101st (cap filled mid-checkout) must fail the coupon cleanly (full price or block) via the existing exactly-once charge/refund path. CC implements (backend+frontend) → Codex gate → Guy approve. **Cap-race design = Option 1 Reserve-at-checkout / LEASE (Guy+Cowork 2026-07-06):** atomically hold 1 of 100 at discounted-checkout creation, confirm on payment success, TTL auto-release for unpaid/abandoned holds → cap never exceeded AND nobody is charged-then-refunded (avoids the fragile PayMe refund path). Chosen over count-on-success+refund-overflow specifically to eliminate charge-then-refund on F&F. Requirement: TTL release + atomic hold→confirm (CAS). **DELIVERED on `feat/coupon-code`** (`5c0c6fdf` backend + `3a1e8641` frontend, pushed, green 1353/19; lease model; NOT self-certified). Awaiting: (1) **Codex mandatory money/concurrency gate** (CC has a review brief + a DB-gated race proof `coupon-cap.staging.spec.ts` to run vs staging); (2) config CONFIRMED `FIRST100`/25%/100 (Guy 2026-07-06; seeded INACTIVE); (3) apply migration `20260707_add_coupon_code` to STAGING only (NOT prod); (4) go-live blocked by OQ-PA4. Nothing enabled — coupon inactive, payments waitlisted. **Codex round-1 = FAIL 2026-07-06:** cap-breaking hole — `confirmCouponForOrder` confirms an EXPIRED hold without re-checking expiry/capacity (`coupon-service.ts:170-189`), and PayMe sets no sale expiry (`payme.ts:218-229`) → paid-after-expiry can push confirmedCount past max (reserve A→expire→B fills cap→late-pay A→over-cap). Fix (routed to CC): (a) cap-safe confirm — re-check capacity+expiry under the Coupon lock, never exceed max, expired+full → paid-late exception/refund; (b) set PayMe sale expiry ≤ lease TTL; (c) add DB proof (cap=1, reserve A, expire A, reserve+confirm B, confirm A → never exceeds 1). All else PASSED. → Codex re-gate.
- **OQ-PA1:** Is the PayMe refund exactly-once design Codex-approved for launch, or still oscillating?
- **OQ-PA2:** Manual refund + support path — confirmed to exist and documented?

## UX / UI
- **OQ-U1:** Wizard mobile/summary final state — merged and launch-ready?
- **OQ-U2:** Order confirmation / ready / failure notifications — do they exist and are they on-brand?

## QA / PROD
- **OQ-Q1:** Admin visibility (QA queue / order dashboard) for supervised launch — exists?
- **OQ-Q2:** PROD env-var checklist owner + verification before charging.

## Launch / support
- **OQ-L1:** Intake throttle mechanism (waitlist/cap) for "first 30 families" — built or manual?
- **OQ-L2:** Go/no-go date confidence for 07-15 given engine work — realistic or slip?
- **OQ-L3:** Authorize ClickUp (+ other) connectors so Operator can sync Project OS ↔ tracker. (blocks 04 specifics)
