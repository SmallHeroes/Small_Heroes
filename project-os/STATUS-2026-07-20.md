# STATUS — 2026-07-20 · pivot back to product

## Overview

Two things changed today. The pipeline finally **merged to main** — `main` and `feat/chunked-generation` are now the same commit (`1ac9026d`) after being 330 apart. And production was **rebuilt clean** in a new Frankfurt project after we discovered the old "production" had never been live.

**Nothing is stranded.** Every branch is pushed, main is current, and there is exactly one unmerged branch — the Human-QA console, which is finished and waiting for review.

Production is deployed but **not yet functional**: every server function returns 500 because PayMe webhook verification is unconfigured, and that is blocked on PayMe account approval. That is the right place to pause. **The product track does not depend on any of it.**

---

## Git hygiene — clean

| Branch | Head | State |
|---|---|---|
| `main` | `1ac9026d` | Current. Fully caught up with the pipeline. |
| `feat/chunked-generation` | `1ac9026d` | Identical to main. Nothing unpushed. |
| `feat/human-qa-console` | `94483492` | **2 ahead — the only outstanding work.** Complete, read-only, awaiting review. |

Nothing is mid-flight. No uncommitted work, no unpushed commits, no half-finished unit.

---

## What the last stretch actually bought

Six Codex rounds on the delivery path found defects that would each have hit a real paying customer:
- A safety-held book could be **shipped automatically** — twice, on two different paths.
- A customer could be **emailed the same book twice**.
- A force-ship window existed on **the path every book takes**, not just an admin route.
- Child photos were never cleaned up when an order **failed** — a permanent leak, now closed.
- The environment guard was protecting a **deleted project**, treating real production as safe to write.

The delivery fence, the human-QA lifecycle, the structural funnel guard, and the 21-test real-Postgres harness are all product code and all survive. This was not detour work.

**What was wasted:** the Reconciliation Bridge and the quarantine — roughly a day, caused by Cowork asserting that 244 dev orders were live customer data without ever asking. Codex ruled both void.

---

## Where each track stands

**Production — paused, correctly.**
Database built clean and verified (46 migrations, 30 tables, RLS on all, zero `anon` grants, `book-images` bucket created). Vercel points at it, functions pinned to `fra1`. Blocked only on `PAYME_WEBHOOK_SECRET` / `PAYME_WEBHOOK_ALLOWED_IPS`, which needs the PayMe account approved. **Do not invent a value** — a wrong secret makes the app reject every real payment webhook with a 403, silently.

**Human-QA console — done, needs your eyes.**
Cursor delivered `/admin/review` (list) and `/admin/review/[orderId]` (detail) — admin-gated, read-only, no customer access keys, with four screenshots including empty and held states. Unmerged.

**Codex — three items open.**
Round-6 units A/B/C/D re-gate; the quarantine hardening re-gate (now void — it can be withdrawn); and the premise-correction consult. Only the first still matters.

**Product — where we resume.**

---

## The product gap, stated plainly

On 2026-07-16 the stated priority was *"first character and set continuity — that is mandatory"*. The Set Identity Board was built, minted, and **you approved the fox board visually**. But it has **never been proven on a full book render**. The one attempt stalled at 11/12 pages, and the board flag was never confirmed active on the environment that rendered it.

So the single most important product question is still unanswered: **does the board actually hold the room, the child and the companion consistent across twelve pages of a real book?**

Everything else in the product track sits behind that answer.

---

## Recommended order

**1. Merge the console.** It is finished, read-only, and touches nothing in the delivery path. Leaving it unmerged is the only branch sprawl we have.

**2. Render one full fox book on QA, with the board on.** Not a five-page sample — the whole book. This is the proof that never happened. Confirm `SET_IDENTITY_BOARD=true` on Preview before starting, and confirm the render completes rather than stalling.

**3. Review it as a product, not as a system.** Room identity across pages. Child likeness and wardrobe. Companion presence and signature. Story: is the child the active hero, is there a real choice, is there humour a child would actually laugh at, does the ending earn itself.

**4. Name the defects precisely, then fix generally.** The rule that has held all year: if a fix only helps one story, it is the wrong fix.

**5. Only then return to PayMe and the production canary.**

---

## Carried forward — do not lose

- Unit 1b: private bucket + signed URLs for child photo *sources* (the flow change; the cleanup is done).
- The held RLS/grants migration for staging.
- Staging rebuild from the clean chain (Codex ruled rebuild, not repair — its migration history was hand-seeded).
- PayMe webhook verification, correctly configured and proven against a real test payment, before the first customer.
- Vercel Deployment Protection must be **off** before real payments — it blocks inbound webhooks.
