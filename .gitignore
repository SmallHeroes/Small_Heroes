# Next.js
.next/
out/
.vercel

# Dependencies
node_modules/

# TypeScript
*.tsbuildinfo

# Local / secrets (never commit)
.env
.env.local
.env.*.local

# Debug / scratch
debug-*

# OS / tooling noise
.tmp.driveupload/
.tmp.drivedownload/

# Scratch, QA, proof runs
tmp/
story-qa-logs/
phase2-logs/
test-results/
test-outputs/
proof-output/
.tmp_story_*.html
.tmp_story_headers.txt
public/qa-capture/

# Experiments (re-runnable, not source)
experiments/

# LoRA training data — large binaries, regenerable
lora-training-data/

# Reference images (large, stored externally)
Refs/

# Story bank review spreadsheets (regenerable)
story-bank/*.xlsx

# Voice / audio test files
voice-samples/
scripts/test-audio*
scripts/test-voice*
scripts/check-voices*

# Script run logs (never commit)
scripts/*.log

# Temp files
zi*
image-experiment-1/
recipe-output/

# Working artifacts / handoff docs (local only, not source, regenerable)
outputs/
briefs/
docs/
1.txt
